---
phase: 02-exam-content
plan: "02"
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/flashcards.ts
  - src/data/questions.ts
  - src/data/content.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-05]
must_haves:
  truths:
    - "Each domain has at least its target flashcard count (D1≥13, D2≥9, D3≥10, D4≥10, D5≥8; total ≥50)"
    - "Each domain has at least its target question count (D1≥11, D2≥7, D3≥8, D4≥8, D5≥6; total ≥40)"
    - "Every flashcard spans all 5 domains and is tagged by domain"
    - "All new questions keep the 1-correct + 3-distractor + explanation contract"
    - "The 12 official-sample questions from 02-01 are untouched (still exactly 12)"
  artifacts:
    - path: "src/data/flashcards.ts"
      provides: "Expanded deck of ≥50 flashcards meeting per-domain minimums"
      contains: "Flashcard[]"
    - path: "src/data/questions.ts"
      provides: "Expanded bank of ≥40 questions meeting per-domain minimums"
      contains: "Question[]"
  key_links:
    - from: "src/data/content.test.ts"
      to: "src/data/content.ts selectors"
      via: "per-domain count assertions"
      pattern: "flashcardCountsByDomain|questionCountsByDomain"
---

<objective>
Expand the seed dataset authored in 02-01 up to the per-domain minimums in 02-CONTEXT.md so the
content volume visibly tracks the exam weights (D1 27%, D2 18%, D3 20%, D4 20%, D5 15%).

Purpose: CONT-05 requires per-domain volume to track weights, and CONT-01/CONT-02 require coverage
across all 5 domains. The seed (15 cards / 26 questions) is short of the ≥50 / ≥40 minimums; this
plan authors the additional grounded, exam-style content. (CONT-01, CONT-02, CONT-03, CONT-05)

Output: Updated `src/data/flashcards.ts` (≥50 cards) and `src/data/questions.ts` (≥40 questions),
with strengthened per-domain count tests in `src/data/content.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-exam-content/02-CONTEXT.md
@.planning/phases/02-exam-content/02-01-SUMMARY.md
@src/data/types.ts
@src/data/flashcards.ts
@src/data/questions.ts
@src/data/content.ts
@src/data/content.test.ts

<authoring_targets>
<!-- From 02-CONTEXT.md per-domain volume targets (minimums). -->
| Domain | Weight | Flashcards (min) | Questions (min) | Seed flashcards | Seed questions |
|--------|--------|------------------|-----------------|-----------------|----------------|
| D1     | 27%    | 13               | 11              | 4               | 5 (q1,q2,q3,sq1,sq7) |
| D2     | 18%    | 9                | 7               | 3               | 4 (q4,q5,q6,sq2,sq9) → 5 |
| D3     | 20%    | 10               | 8               | 3               | 7 (q7,q8,q9,sq4,sq5,sq6,sq10) |
| D4     | 20%    | 10               | 8               | 3               | 5 (q10,q11,q12,sq11,sq12) |
| D5     | 15%    | 8                | 6               | 2               | 4 (q13,q14,sq3,sq8) |
| Total  | 100%   | 50               | 40              | 15              | 26             |
<!-- NOTE counts above include both source:'original' and source:'official-sample' questions. -->
</authoring_targets>

<authoring_quality_bar>
<!-- From 02-CONTEXT.md — favor these correct-answer themes when writing originals. -->
Distractors must be plausible-but-wrong the way the guide models. Recurring correct-answer themes:
deterministic enforcement (hooks/prerequisites) > prompt; better tool descriptions > added
complexity; explicit criteria + few-shot > self-reported confidence; least-privilege tool access;
structured errors > generic; right API for latency need (sync vs Batch); attention dilution in
large contexts (split passes); context discipline (trim tool output, compaction, citation/provenance).
Ground each new card/question in the domain's exam task statements; match the established voice.
</authoring_quality_bar>
</context>

<feature>
  <name>Expand deck + bank to per-domain weight-tracking minimums</name>
  <files>src/data/flashcards.ts, src/data/questions.ts, src/data/content.test.ts</files>
  <behavior>
    After expansion, the loader's per-domain counts must satisfy:
    - flashcardCountsByDomain(): d1≥13, d2≥9, d3≥10, d4≥10, d5≥8 ; total ≥50
    - questionCountsByDomain(): d1≥11, d2≥7, d3≥8, d4≥8, d5≥6 ; total ≥40
    - official-sample questions STILL === 12 (none removed/retagged)
    - every NEW question still has 4 options, correct 0..3, non-empty whyCorrect/whyOthers
    - every NEW flashcard has non-empty front/back and a valid DomainId
    - all ids remain unique across the arrays
  </behavior>
  <implementation>
    RED: tighten content.test.ts to per-domain minimum assertions (will fail at seed counts).
    GREEN: author additional original cards/questions until minimums are met.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Tighten content tests to per-domain minimums</name>
  <read_first>
    - src/data/content.test.ts (existing invariant tests from 02-01)
    - src/data/content.ts (flashcardCountsByDomain / questionCountsByDomain signatures)
    - .planning/phases/02-exam-content/02-CONTEXT.md (per-domain volume targets table)
  </read_first>
  <action>
    1. In `src/data/content.test.ts`, REPLACE the fixed `flashcards === 15` / `questions === 26`
       assertions from 02-01 with per-domain minimum assertions and total minimums:
         - const FC_MIN = { d1:13, d2:9, d3:10, d4:10, d5:8 }
         - const Q_MIN  = { d1:11, d2:7, d3:8, d4:8, d5:6 }
       Assert for each domain `flashcardCountsByDomain()[d] >= FC_MIN[d]` and
       `questionCountsByDomain()[d] >= Q_MIN[d]`; assert totals `getFlashcards().length >= 50` and
       `getQuestions().length >= 40`. KEEP the official-sample === 12 test and the per-question
       shape tests unchanged.
    2. Add a uniqueness test: ids across flashcards are unique, ids across questions are unique.
    3. Run `npm test` — confirm the new per-domain/total assertions FAIL (seed is below minimums).
    4. Commit: `test(02-02): require per-domain weight-tracking minimums (TDD red)`
  </action>
  <acceptance_criteria>
    - `grep -q "FC_MIN" src/data/content.test.ts &amp;&amp; grep -q "Q_MIN" src/data/content.test.ts`
    - `grep -Eq ">= ?50" src/data/content.test.ts &amp;&amp; grep -Eq ">= ?40" src/data/content.test.ts`
    - `grep -q "official-sample" src/data/content.test.ts` (12-count test retained)
    - `npm test` exits NON-zero (RED — seed below minimums)
    - `git log --oneline -1` subject starts with `test(02-02)`
  </acceptance_criteria>
  <verify>
    <automated>npm test; test $? -ne 0 &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>content.test.ts now asserts per-domain minimums + ≥50/≥40 totals + id uniqueness and fails at seed volume; RED commit made.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Author original cards + questions to meet minimums</name>
  <read_first>
    - src/data/flashcards.ts, src/data/questions.ts (the seed authored in 02-01 — match its voice/format)
    - .planning/phases/02-exam-content/02-CONTEXT.md (authoring quality bar + per-domain targets)
    - src/data/types.ts (Flashcard/Question contracts — new entries must satisfy them)
  </read_first>
  <action>
    1. Append NEW flashcards to `src/data/flashcards.ts` (continue id numbering f16, f17, …) until
       each domain meets its minimum. Required NEW cards per domain (to reach the minimums from the
       seed counts): D1 +9, D2 +6, D3 +7, D4 +7, D5 +6 (≥35 new → ≥50 total). Each card:
       `{ id, domain, front, back }` — front = a concept/prompt, back = a crisp, grounded
       explanation in the established voice. Ground topics in the domain's exam task statements
       (e.g. D1: orchestrator-worker, evaluator-optimizer, stop conditions, handoffs; D2: tool
       schemas, error shapes, pagination, MCP resources/prompts; D3: subagents, /commands, rules
       globs, plan mode, permissions; D4: prefill, XML tags, few-shot, output schemas, injection
       defense; D5: compaction, grounding/citation, retries with structured errors, batch vs sync).
    2. Append NEW questions to `src/data/questions.ts` (continue ids q15, q16, … ; all
       `source: 'original'`) until each domain meets its question minimum. Required NEW questions
       per domain: D1 +6, D2 +2, D3 +1, D4 +3, D5 +2 (≥14 new → ≥40 total). Each question:
       exactly 4 options (4-tuple), one `correct` in 0..3, plus `whyCorrect` and `whyOthers`
       grounded in the authoring quality bar's correct-answer themes. Optionally tag `scenario`
       from the 6 official scenario names where it fits. Do NOT touch sq1..sq12 or q1..q14.
    3. Run `npm test` — all per-domain minimums, totals, uniqueness, shape, and the official-sample
       === 12 invariant PASS. Adjust counts (add more) if any domain is short; fix data, not tests.
    4. Run `npm run typecheck` and `npm run build` — both exit 0.
    5. Commit: `feat(02-02): expand deck + bank to per-domain weight-tracking minimums (TDD green)`
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f" src/data/flashcards.ts` >= 50
    - `grep -c "id: 'q" src/data/questions.ts` >= 40 minus the 12 sq ids — i.e. `grep -cE "id: '(q|sq)" src/data/questions.ts` >= 40
    - `grep -c "source: 'official-sample'" src/data/questions.ts` returns 12 (unchanged)
    - `npm test` exits 0 (per-domain minimums + ≥50/≥40 totals + uniqueness all GREEN)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `git log --oneline -1` subject starts with `feat(02-02)`
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>Deck ≥50 cards and bank ≥40 questions, each domain at/above its minimum, official-sample still 12, all tests + typecheck + build pass; GREEN commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time module graph → runtime | Same as 02-01: static authored content, no runtime input boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | expanded content arrays | accept | Authored static content; the per-question shape test (4 options, correct 0..3, non-empty explanations) guards against malformed entries silently entering the bank. |
</threat_model>

<verification>
- `npm test` passes (per-domain minimums, ≥50/≥40 totals, id uniqueness, official-sample === 12)
- `npm run typecheck` exits 0
- `npm run build` exits 0
</verification>

<success_criteria>
- Each domain meets its flashcard and question minimum so volume tracks exam weights (CONT-05)
- All 5 domains covered for both flashcards and questions (CONT-01, CONT-02)
- New questions keep the 1-correct + 3-distractor + explanation contract (CONT-03)
- The 12 official-sample questions remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/02-exam-content/02-02-SUMMARY.md`
</output>
