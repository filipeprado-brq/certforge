---
phase: 02-exam-content
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/scenarios.ts
  - src/data/flashcards.ts
  - src/data/questions.ts
  - src/data/content.ts
  - src/data/content.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-06]
must_haves:
  truths:
    - "Content imports resolve at build time with no network fetch (no fetch/XHR/import() in data modules)"
    - "Every question has exactly 4 options, a correct index in 0..3, and non-empty whyCorrect + whyOthers"
    - "Questions tagged source:'official-sample' number exactly 12 and carry the guide's domain + scenario tags"
    - "A typed loader exposes getFlashcards, getQuestions, and per-domain counts"
  artifacts:
    - path: "src/data/types.ts"
      provides: "Flashcard, Question, Scenario, Source type contracts"
      contains: "interface Question"
    - path: "src/data/scenarios.ts"
      provides: "The 6 official scenario names as a const tuple"
      contains: "SCENARIOS"
    - path: "src/data/flashcards.ts"
      provides: "Seed flashcards (15) ported from design/data.jsx, typed"
      contains: "Flashcard[]"
    - path: "src/data/questions.ts"
      provides: "Seed questions (14) + 12 official-sample questions, typed"
      contains: "Question[]"
    - path: "src/data/content.ts"
      provides: "Typed loader/selectors: getFlashcards, getQuestions, flashcardCountsByDomain, questionCountsByDomain"
      exports: ["getFlashcards", "getQuestions", "flashcardCountsByDomain", "questionCountsByDomain"]
  key_links:
    - from: "src/data/content.ts"
      to: "src/data/flashcards.ts + src/data/questions.ts"
      via: "static import"
      pattern: "import.*from './flashcards'"
    - from: "src/data/questions.ts"
      to: "src/data/types.ts"
      via: "import Question type"
      pattern: "import.*Question.*from './types'"
---

<objective>
Establish the Phase 2 content data layer: shared TS types, the 6 scenarios, the typed
loader/selectors, and the SEED dataset (15 flashcards + 14 questions ported verbatim from
`design/data.jsx`) PLUS the 12 official sample questions added verbatim from 02-CONTEXT.md.

Purpose: This is the contract every later plan (02-02 expansion, 02-03 browse views, and Phases
3/4) builds on. Getting types + loader + invariant tests right first prevents a scavenger hunt
later. (CONT-02, CONT-03, CONT-04, CONT-06)

Output: `src/data/types.ts`, `src/data/scenarios.ts`, `src/data/flashcards.ts`,
`src/data/questions.ts`, `src/data/content.ts`, `src/data/content.test.ts`. All content embedded
as imported TS modules — no network fetch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-exam-content/02-CONTEXT.md
@design/data.jsx
@src/data/domains.ts

<interfaces>
<!-- Existing, reuse — DO NOT redefine DomainId/Domain. -->
From src/data/domains.ts:
```typescript
export type DomainId = 'd1' | 'd2' | 'd3' | 'd4' | 'd5'
export interface Domain { id: DomainId; code: string; short: string; name: string; weight: number }
export const DOMAINS: Domain[]
```

<!-- Target type shapes to CREATE in src/data/types.ts (from 02-CONTEXT.md <decisions>): -->
```typescript
export type Scenario =
  | 'Customer Support Resolution Agent'
  | 'Code Generation with Claude Code'
  | 'Multi-Agent Research System'
  | 'Developer Productivity with Claude'
  | 'Claude Code for Continuous Integration'
  | 'Structured Data Extraction'
export type Source = 'official-sample' | 'original'
export interface Flashcard { id: string; domain: DomainId; front: string; back: string }
export interface Question {
  id: string
  domain: DomainId
  scenario?: Scenario
  stem: string
  options: [string, string, string, string]
  correct: 0 | 1 | 2 | 3
  whyCorrect: string
  whyOthers: string
  source?: Source
}
```
</interfaces>
</context>

<feature>
  <name>Typed embedded content layer with seed + 12 official questions</name>
  <files>src/data/types.ts, src/data/scenarios.ts, src/data/flashcards.ts, src/data/questions.ts, src/data/content.ts, src/data/content.test.ts</files>
  <behavior>
    The loader's selectors and the dataset must satisfy these testable invariants:
    - getQuestions().filter(q => q.source === 'official-sample').length === 12
    - getFlashcards().length === 15 (seed only, in THIS plan; 02-02 expands)
    - getQuestions().length === 26 (14 seed + 12 official, in THIS plan; 02-02 expands)
    - Every question: q.options.length === 4, q.correct is integer 0..3, q.whyCorrect.trim() !== '', q.whyOthers.trim() !== ''
    - Every flashcard: f.front.trim() !== '' && f.back.trim() !== '' && DOMAINS has f.domain
    - getQuestions({domain:'d3'}) returns only d3 questions; getQuestions({scenario:'Multi-Agent Research System'}) returns only that scenario
    - flashcardCountsByDomain() and questionCountsByDomain() return Record<DomainId, number> summing to the array lengths
    - No data module contains the substrings 'fetch(' , 'XHR', 'axios', or dynamic 'import(' (embedded only — CONT-06)
  </behavior>
  <implementation>
    Build via RED -> GREEN. Write content.test.ts FIRST (RED), then author the data + loader (GREEN).
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Define types, scenarios, and failing invariant tests</name>
  <read_first>
    - src/data/domains.ts (reuse DomainId/Domain — do NOT redefine)
    - .planning/phases/02-exam-content/02-CONTEXT.md (the &lt;decisions&gt; type shapes + the verifiability hints)
    - vite.config.ts (vitest globals:true, jsdom — tests use describe/it/expect globals)
    - src/lib/storage.test.ts (existing vitest style reference)
  </read_first>
  <action>
    1. Create `src/data/types.ts` exporting EXACTLY the Scenario union, Source union, Flashcard
       interface, and Question interface shown in the &lt;interfaces&gt; block above. Import DomainId
       from './domains' (`import type { DomainId } from './domains'`). Do NOT redefine DomainId.
    2. Create `src/data/scenarios.ts`: `export const SCENARIOS = [ ...the 6 names... ] as const`
       using the 6 strings from design/data.jsx SCENARIOS (verbatim). Export
       `export type ScenarioName = typeof SCENARIOS[number]` if convenient, but the canonical
       Scenario type lives in types.ts.
    3. Create `src/data/content.test.ts` with the failing tests encoding every invariant in
       &lt;behavior&gt; above. Import from './content' (which does not yet exist — RED). Use the exact
       count assertions for THIS plan: flashcards === 15, questions === 26, official-sample === 12.
       Add a test that reads the data module source text via Node fs and asserts none of
       'fetch(', 'XHR', 'axios', 'import(' appear in flashcards.ts/questions.ts/content.ts
       (use `import { readFileSync } from 'node:fs'` with an absolute path via `new URL(..., import.meta.url)`).
    4. Run `npm test` — confirm content.test.ts FAILS (module './content' not found / undefined).
    5. Commit: `test(02-01): add failing content-layer invariant tests (TDD red)`
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/data/types.ts`
    - `grep -c "interface Question" src/data/types.ts` returns 1
    - `grep -c "interface Flashcard" src/data/types.ts` returns 1
    - `grep -q "import type { DomainId }" src/data/types.ts` (reuses existing type)
    - `test -f src/data/scenarios.ts` and `grep -c "SCENARIOS" src/data/scenarios.ts` >= 1
    - `grep -c "Customer Support Resolution Agent" src/data/scenarios.ts` returns 1
    - `test -f src/data/content.test.ts`
    - `grep -E "official-sample.*length.*12|length.*12" src/data/content.test.ts` matches (12-count assertion present)
    - `npm test` exits NON-zero (RED) with content.test.ts failing
    - `git log --oneline -1` subject starts with `test(02-01)`
  </acceptance_criteria>
  <verify>
    <automated>npm test; test $? -ne 0 &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>types.ts + scenarios.ts exist; content.test.ts encodes all invariants and fails because content.ts/data are not yet authored; RED commit made.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Author seed dataset + 12 official questions + typed loader</name>
  <read_first>
    - design/data.jsx (FLASHCARDS = 15 cards; QUESTIONS = 14 questions — port verbatim incl. whyCorrect/whyOthers; IGNORE HISTORY/QUIZ_MODES)
    - .planning/phases/02-exam-content/02-CONTEXT.md &lt;specifics&gt; (the 12 official questions SQ1..SQ12 — VERBATIM source for CONT-04)
    - src/data/types.ts, src/data/scenarios.ts (the contracts written in Task 1)
    - src/data/content.test.ts (the invariants to satisfy)
  </read_first>
  <action>
    1. Create `src/data/flashcards.ts`: `import type { Flashcard } from './types'` then
       `export const FLASHCARDS: Flashcard[] = [...]`. Port ALL 15 cards from design/data.jsx
       FLASHCARDS verbatim (ids f1..f15, same domain tags, same front/back text — keep the unicode
       text intact). No `mastery`/`cardsDue` fields (those belong to Phase 3 progress, not content).
    2. Create `src/data/questions.ts`: `import type { Question } from './types'` then
       `export const QUESTIONS: Question[] = [...]`. In order:
       a. Port the 14 seed questions from design/data.jsx QUESTIONS verbatim (ids q1..q14, same
          domain/scenario/options/correct/whyCorrect/whyOthers). Add `source: 'original'` to each.
          Ensure `options` is a 4-tuple literal so it satisfies `[string,string,string,string]`.
       b. Append the 12 official questions from 02-CONTEXT.md &lt;specifics&gt;, ids `sq1`..`sq12`,
          each with `source: 'official-sample'` and the EXACT domain + scenario + correct index +
          whyCorrect + whyOthers given in the spec. Mapping (domain · scenario · correct):
            - sq1  d1 · 'Customer Support Resolution Agent' · 0
            - sq2  d2 · 'Customer Support Resolution Agent' · 1
            - sq3  d5 · 'Customer Support Resolution Agent' · 0
            - sq4  d3 · 'Code Generation with Claude Code' · 0
            - sq5  d3 · 'Code Generation with Claude Code' · 0
            - sq6  d3 · 'Code Generation with Claude Code' · 0
            - sq7  d1 · 'Multi-Agent Research System' · 1
            - sq8  d5 · 'Multi-Agent Research System' · 0
            - sq9  d2 · 'Multi-Agent Research System' · 0
            - sq10 d3 · 'Claude Code for Continuous Integration' · 0
            - sq11 d4 · 'Claude Code for Continuous Integration' · 0
            - sq12 d4 · 'Claude Code for Continuous Integration' · 0
          Transcribe each stem and all 4 options verbatim (the A/B/C/D text). Build whyCorrect from
          the spec's "whyCorrect" line and whyOthers from the spec's "whyOthers" line, verbatim.
    3. Create `src/data/content.ts`: `import { FLASHCARDS } from './flashcards'`,
       `import { QUESTIONS } from './questions'`, `import { DOMAINS, type DomainId } from './domains'`.
       Export:
         - `getFlashcards(opts?: { domain?: DomainId }): Flashcard[]`
         - `getQuestions(opts?: { domain?: DomainId; scenario?: Scenario }): Question[]`
         - `flashcardCountsByDomain(): Record<DomainId, number>`
         - `questionCountsByDomain(): Record<DomainId, number>`
       Implement with plain `.filter`/`.reduce` over the imported arrays. NO fetch/XHR/dynamic import.
    4. Run `npm test` — all content.test.ts invariants PASS. If a count is off, fix the data, not the test.
    5. Run `npm run typecheck` and `npm run build` — both exit 0 (the 4-tuple `options` and
       `correct: 0|1|2|3` literals must type-check; widen with `as const` or explicit tuple typing if needed).
    6. Commit: `feat(02-01): typed content layer — seed deck + 12 official questions + loader (TDD green)`
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f" src/data/flashcards.ts` returns 15
    - `grep -c "source: 'official-sample'" src/data/questions.ts` returns 12
    - `grep -c "source: 'original'" src/data/questions.ts` returns 14
    - `grep -cE "id: 'sq([1-9]|1[0-2])'" src/data/questions.ts` returns 12
    - `grep -c "id: 'q" src/data/questions.ts` returns 14
    - `grep -Eq "fetch\(|XHR|axios|import\(" src/data/flashcards.ts src/data/questions.ts src/data/content.ts; test $? -ne 0` (no network/dynamic-import primitives — CONT-06)
    - `grep -q "export function getFlashcards" src/data/content.ts` (or `export const getFlashcards`)
    - `grep -q "export function getQuestions" src/data/content.ts`
    - `grep -q "questionCountsByDomain" src/data/content.ts && grep -q "flashcardCountsByDomain" src/data/content.ts`
    - `npm test` exits 0 (all invariants GREEN)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `git log --oneline -1` subject starts with `feat(02-01)`
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <done>15 seed flashcards + 14 seed questions (source:'original') + 12 official questions (source:'official-sample', ids sq1..sq12) authored; loader selectors implemented; all invariant tests, typecheck, and build pass; GREEN commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time module graph → runtime | Content is static TS imported into the bundle; no runtime input crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/data/*.ts content modules | accept | Static authored content compiled into the bundle; no external/user input parsed at runtime. The CONT-06 "no fetch/XHR/import()" grep gate doubles as an assurance that no dynamic remote content can be injected. |
| T-02-02 | Information disclosure | content arrays | accept | Content is public study material by design (the app is offline, no secrets in the dataset). |
</threat_model>

<verification>
- `npm test` passes (all content invariants green, including official-sample === 12)
- `npm run typecheck` exits 0
- `npm run build` exits 0
- No fetch/XHR/axios/dynamic-import in any data module
</verification>

<success_criteria>
- Types + scenarios + seed data + 12 official questions + loader all exist and are typed
- official-sample questions number exactly 12 with correct domain/scenario tags (CONT-04)
- Every question has 4 options, correct in 0..3, non-empty explanations (CONT-03)
- Loader selectors + per-domain count functions exported for downstream consumers (CONT-02, CONT-06)
</success_criteria>

<output>
After completion, create `.planning/phases/02-exam-content/02-01-SUMMARY.md`
</output>
