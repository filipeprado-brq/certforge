---
phase: 04-quiz-engine-modes
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/screens/QuizResults.tsx
  - src/screens/QuizFlow.tsx
  - src/screens/HistoryScreen.tsx
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements: [QUIZ-06, QUIZ-07]
must_haves:
  truths:
    - "After any quiz the user sees a results summary: score, per-domain breakdown, and review of missed questions"
    - "Timed results show a ScoreDial with the 720 mark and a PassChip; other modes show raw correct/total + %"
    - "Finishing a quiz records an attempt that appears in the History view"
    - "The History view lists past attempts and opening one shows its results review (missed questions rebuilt from content, never empty)"
    - "Quiz route flows ModeSelect → Runner → Results (QuizBrowse no longer shown)"
  artifacts:
    - path: "src/screens/QuizResults.tsx"
      provides: "Results summary (ScoreDial/PassChip for timed, %/raw otherwise, DomainBars, missed review)"
      contains: "DomainBars"
    - path: "src/screens/QuizFlow.tsx"
      provides: "Stateful Quiz route container wiring ModeSelect→QuizRunner→QuizResults + addAttempt"
    - path: "src/screens/HistoryScreen.tsx"
      provides: "Attempt list + open-to-review (replaces the placeholder)"
  key_links:
    - from: "src/screens/QuizFlow.tsx"
      to: "src/lib/useStoredState.ts"
      via: "addAttempt(attempt) on quiz finish"
      pattern: "addAttempt"
    - from: "src/App.tsx"
      to: "src/screens/QuizFlow.tsx + src/screens/HistoryScreen.tsx"
      via: "route === 'quiz' renders QuizFlow; route === 'history' renders HistoryScreen"
      pattern: "QuizFlow|HistoryScreen"
---

<objective>
Close the phase: build the results summary and the history view, wire the Quiz route to the full
ModeSelect → QuizRunner → QuizResults flow (replacing QuizBrowse), replace the History placeholder,
record finished attempts into quizHistory, and update the two App.test assertions that still expect
the old screens (the same test-breakage trap as Phases 2 & 3).

Purpose: Deliver QUIZ-06 (results summary) and QUIZ-07 (recorded + viewable history) and complete
the v1 roadmap.
Output: `QuizResults.tsx`, `QuizFlow.tsx` (route container), `HistoryScreen.tsx`, updated `App.tsx`
and `App.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-quiz-engine-modes/04-CONTEXT.md
@.planning/phases/01-app-shell-persistence/01-UI-SPEC.md

<interfaces>
<!-- From Plans 04-01 and 04-02 (implemented when this runs). -->
From src/lib/quiz.ts:
```typescript
export interface GradeResult { correct: number; total: number; perDomain: Partial<Record<DomainId, DomainScore>>; missed: Question[] }
export interface DomainScore { correct: number; total: number; pct: number }
export type Answers = Record<string, number | null>
export function scaledScore(correct: number, total: number): number
export function isPass(scaled: number): boolean
```
From src/lib/storage.ts:
```typescript
export interface QuizAttempt {
  id: string; date: string; mode: string
  modeKey: 'scenario'|'domain'|'timed'|'free'
  correct: number; total: number; scaled?: number; pass?: boolean
  perDomain: Record<DomainId, number>            // FLAT pct per domain (number, not {correct,total,pct})
  missed?: Array<{ questionId: string; selected: number | null }>   // ONLY ids+selection are stored
}
```
From src/lib/useStoredState.ts:
```typescript
// hook now returns: { ..., quizHistory: QuizAttempt[], addAttempt: (a: QuizAttempt) => void, resetAll }
```
From src/data/content.ts:
```typescript
export function getQuestions(opts?: { domain?: DomainId; scenario?: Scenario }): Question[]
// used to LOOK UP a Question by id when rebuilding a stored attempt's missed review
```
From src/screens/ModeSelect.tsx: `ModeSelect`, `QuizConfig`.
From src/screens/QuizRunner.tsx: `QuizRunner` with onFinish(grade, questions, answers).
From src/components/QuizParts.tsx: `ScoreDial`, `DomainBars`, `PassChip`.
From src/data/quizModes.ts: `QUIZ_MODES`.

SHAPE-MISMATCH NOTE (critical for executors):
- The ENGINE's `GradeResult.perDomain` is `Partial<Record<DomainId,{correct,total,pct}>>`.
- The STORED `QuizAttempt.perDomain` and the `DomainBars` prop are a FLAT `Record<DomainId, number>` (pct only).
- The UI MUST convert: `grade.perDomain[d].pct` → flat `{ [d]: pct }` before passing to DomainBars
  AND before building a QuizAttempt to store. Never pass the engine's object shape directly.

Existing App.tsx wiring (to modify): route union `'home'|'flashcards'|'quiz'|'history'|'settings'`;
currently `quiz → <QuizBrowse/>` and `history → <Placeholder title="History"/>`. resetAll already
returns to home.
</interfaces>

<design_reference>
- design/screens-quiz.jsx Results (lines 293-373): ScoreDial when scaled present else big %, DomainBars, missed review (your answer + correct + explanation), Retry + Home. PORT to TSX.
- design/screens-home.jsx History (lines 108-140): page head, EmptyState when no attempts, else attempt-row list (date, mode, scaled/raw, PassChip for timed). PORT to TSX; do NOT import the seeded HISTORY — start empty.
</design_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuizResults screen (timed ScoreDial vs raw %, DomainBars, missed review)</name>
  <files>src/screens/QuizResults.tsx</files>
  <read_first>
    - design/screens-quiz.jsx lines 293-373 (Results reference)
    - src/components/QuizParts.tsx (ScoreDial, DomainBars, PassChip)
    - src/lib/quiz.ts (GradeResult, scaledScore, isPass), src/data/types.ts (Question), src/components/Btn.tsx, src/components/DomainBadge.tsx
  </read_first>
  <behavior>
    - Given a timed result (scaled present, pass true): renders ScoreDial with the score and a PassChip; does NOT render a raw "%" headline.
    - Given a non-timed result: renders the raw percentage headline (e.g. "70%") and "X of Y correct".
    - Always renders DomainBars for the per-domain breakdown, built from a FLAT pct map.
    - When there are missed questions, renders a review section: each missed question's stem, the
      user's answer (or "Unanswered"), the correct answer, and the explanation (whyCorrect/whyOthers).
    - Renders a Retry action and a Home/Back action.
  </behavior>
  <action>
    Create src/screens/QuizResults.tsx exporting
    `export interface QuizResultView { modeKey: 'scenario'|'domain'|'timed'|'free'; modeName: string; date: string; questions: Question[]; answers: Answers; grade: GradeResult; scaled?: number; pass?: boolean }`
    and `export function QuizResults({ result, onRetry, onHome, isHistory }: { result: QuizResultView; onRetry?: () => void; onHome: () => void; isHistory?: boolean })`.
    Port design 293-373:
    - Header: ScoreDial when `result.scaled != null` (pass-mark handled inside ScoreDial), else big
      `Math.round(grade.correct/grade.total*100)%` + "X of Y correct".
    - DomainBars: SHAPE CONVERSION (mandatory) — build a FLAT `Record<DomainId, number>` pct map from
      the engine's object shape BEFORE passing to DomainBars:
      `const domainPct: Partial<Record<DomainId, number>> = {};`
      `for (const [d, s] of Object.entries(result.grade.perDomain)) if (s) domainPct[d as DomainId] = s.pct;`
      Pass `domainPct` (numbers) to DomainBars — NEVER pass `result.grade.perDomain` (objects) directly.
      Skip absent domains.
    - Missed review: `result.grade.missed` (Question[]) — for each, show DomainBadge, stem, the user's
      `result.answers[q.id]` option (or "Unanswered (time ran out)" when null), the correct option
      `q.options[q.correct]`, and the .explanation panel (whyCorrect + whyOthers). Use KEY_LABELS=['A','B','C','D'].
    - Actions: "Retry this mode" (only when onRetry given) and onHome ("Back to home" or "Back to history" when isHistory).
    Use existing CSS classes (.card, .explanation, .state-label*, .stat-num, .page-head).
  </action>
  <verify>
    <automated>npm test -- QuizResults</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function QuizResults' src/screens/QuizResults.tsx` === 1
    - `grep -c 'ScoreDial' src/screens/QuizResults.tsx` >= 1 and `grep -c 'DomainBars' src/screens/QuizResults.tsx` >= 1
    - Branch on scaled present vs absent: `grep -c 'scaled' src/screens/QuizResults.tsx` >= 1
    - SHAPE CONVERSION present: the file maps `.pct` into a flat number map before DomainBars
      (`grep -c '\.pct' src/screens/QuizResults.tsx` >= 1) and does NOT pass `grade.perDomain` straight to DomainBars
    - Renders missed review: `grep -c 'missed' src/screens/QuizResults.tsx` >= 1
    - Component test: timed result renders the scaled score + PassChip text (Pass/Fail); non-timed renders "%" — passes
    - Component test: a result with ≥1 missed renders the missed question's stem (review non-empty) — passes
    - `npm run typecheck` exits 0 (proves the flat-vs-object perDomain shapes are not crossed)
  </acceptance_criteria>
  <done>Results shows ScoreDial+PassChip for timed and raw %/total otherwise, DomainBars (fed a flat pct map converted from grade.perDomain[d].pct), and missed-question review with explanations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuizFlow container + HistoryScreen, recording attempts, rebuilding history review</name>
  <files>src/screens/QuizFlow.tsx, src/screens/HistoryScreen.tsx</files>
  <read_first>
    - design/screens-home.jsx lines 108-140 (History reference)
    - src/screens/ModeSelect.tsx (ModeSelect, QuizConfig), src/screens/QuizRunner.tsx (QuizRunner), src/screens/QuizResults.tsx (QuizResults, QuizResultView)
    - src/lib/useStoredState.ts (quizHistory, addAttempt), src/lib/storage.ts (QuizAttempt)
    - src/lib/quiz.ts (scaledScore, isPass, GradeResult, DomainScore), src/data/quizModes.ts (QUIZ_MODES), src/data/domains.ts
    - src/data/content.ts (getQuestions — for rebuilding stored attempts), src/data/types.ts (Question)
    - src/components/EmptyState.tsx, src/components/Btn.tsx, src/components/QuizParts.tsx (PassChip)
  </read_first>
  <behavior>
    - QuizFlow starts on ModeSelect; choosing a mode + Start renders QuizRunner; finishing renders
      QuizResults AND records the attempt (addAttempt is called exactly once per finish).
    - Retry returns to QuizRunner for the same config; Home returns to ModeSelect.
    - The recorded QuizAttempt: id = a generated unique string (e.g. crypto.randomUUID() or Date.now()
      + random — id/date generated in the UI layer, NOT in the engine), date = a display/ISO string,
      mode = QUIZ_MODES name (+ " · Dn" for domain mode), modeKey = config.mode, correct/total from
      grade, scaled/pass set ONLY for timed (scaledScore/isPass), perDomain = FLAT {d: pct} map
      (converted from grade.perDomain[d].pct — see shape note), missed = grade.missed mapped to
      { questionId, selected: answers[id] ?? null }.
    - HistoryScreen with empty quizHistory renders an EmptyState (no "Coming soon"); with attempts,
      renders an attempt-row per attempt (date, mode, scaled "/1000" or raw "X/Y · %", PassChip for timed).
    - Clicking an attempt row opens its QuizResults review (isHistory). The review MUST NOT be empty:
      the stored attempt kept only `missed: {questionId, selected}`, so the review's missed questions
      are REBUILT by looking each questionId up in the content layer (getQuestions) to recover stem,
      options, correct, and explanation. A missed questionId that no longer exists is skipped (no crash).
  </behavior>
  <action>
    Create src/screens/QuizFlow.tsx exporting `export function QuizFlow()`:
    - Uses `useStoredState()` for `addAttempt` (it may re-call the hook; that is fine — storage is the source of truth).
    - Local state machine: `view: 'select' | 'run' | 'results'`, `config: QuizConfig | null`, `lastResult: QuizResultView | null`.
    - `onStart(config)` → set config, view='run'.
    - QuizRunner onFinish(grade, questions, answers):
      * compute scaled/pass when config.mode==='timed' (scaledScore(grade.correct, grade.total), isPass(scaled)).
      * build the FLAT perDomain pct map from grade.perDomain: iterate `Object.entries(grade.perDomain)`,
        set `flat[d] = s.pct` for present domains (the same conversion QuizResults does — keep it consistent).
      * build the QuizResultView (modeName from QUIZ_MODES; for domain mode append " · " + DOMAINS code).
      * build the QuizAttempt (id: `crypto.randomUUID?.() ?? String(Date.now())`, date: `new Date().toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})`,
        perDomain: the FLAT map above, missed: grade.missed.map(q => ({ questionId: q.id, selected: answers[q.id] ?? null }))).
      * call `addAttempt(attempt)` ONCE, set lastResult, view='results'.
    - Render ModeSelect / QuizRunner / QuizResults per view. Retry → view='run' (same config). Home → view='select'.

    Create src/screens/HistoryScreen.tsx exporting `export function HistoryScreen()`:
    - `const { quizHistory } = useStoredState()`.
    - Local `open: QuizAttempt | null`. When `open` is set, BUILD a QuizResultView FROM the stored attempt
      (do not assume the runner's in-memory result still exists):
        * REBUILD the missed Question[] from content: for each `m of (open.missed ?? [])`,
          look the question up — e.g. `const q = getQuestions().find(x => x.id === m.questionId)`;
          if found push `q`, else SKIP (questionId no longer in content → don't crash).
        * REBUILD `answers`: `{ [m.questionId]: m.selected }` for each stored missed entry (so QuizResults
          can render the user's chosen option for each missed question).
        * REBUILD a minimal `grade: GradeResult` for the view: `{ correct: open.correct, total: open.total,
          missed: rebuiltMissedQuestions, perDomain: {} }`. (perDomain detail is not stored; DomainBars in
          a reopened attempt may show the stored flat pct map instead — pass `open.perDomain` to DomainBars
          directly since it is already the flat `Record<DomainId, number>` shape. Do NOT attempt to
          reconstruct the engine's object shape.)
        * `questions` for the view = the rebuilt missed questions (review only needs the missed set).
        * scaled/pass/date/modeName/modeKey come straight from the stored attempt.
      Render `<QuizResults result={view} onHome={() => setOpen(null)} isHistory />`.
      Because missed questions are rebuilt from content, a reopened attempt with ≥1 missed shows a
      NON-EMPTY review (the missed question's stem/options/explanation).
    - Empty list → EmptyState "No attempts yet" with a CTA. Non-empty → attempt-row buttons (port
      design 121-137): date, mode, `scaled != null ? scaled+' / 1000' : correct+'/'+total+' · '+pct+'%'`,
      PassChip when scaled present else a "Practice" label. Clicking sets open.
    NOTE: id/date generation lives here/QuizFlow (UI), keeping the engine pure — do not move it into quiz.ts.
    NOTE: DomainBars in a reopened attempt is fed `open.perDomain` (already a flat number map); the live
    results path feeds the converted flat map from grade.perDomain[d].pct — both are flat number maps.
  </action>
  <verify>
    <automated>npm test -- History</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function QuizFlow' src/screens/QuizFlow.tsx` === 1
    - `grep -c 'addAttempt' src/screens/QuizFlow.tsx` >= 1
    - `grep -cE "scaledScore|isPass" src/screens/QuizFlow.tsx` >= 1 (timed scoring at finish)
    - QuizFlow converts perDomain to a flat pct map: `grep -c '\.pct' src/screens/QuizFlow.tsx` >= 1
    - `grep -c 'export function HistoryScreen' src/screens/HistoryScreen.tsx` === 1
    - HistoryScreen rebuilds missed from content: `grep -c 'getQuestions' src/screens/HistoryScreen.tsx` >= 1
    - `grep -c 'EmptyState' src/screens/HistoryScreen.tsx` >= 1 and `grep -c 'Coming soon' src/screens/HistoryScreen.tsx` === 0
    - `grep -c 'attempt-row' src/screens/HistoryScreen.tsx` >= 1
    - Component test: HistoryScreen with empty store renders EmptyState (no "Coming soon") — passes
    - Component test: HistoryScreen seeded (via stored quizHistory) with one attempt that has ≥1 missed
      entry whose questionId EXISTS in content — clicking the attempt-row renders the missed question's
      STEM (review is non-empty) — passes
    - Component test: an attempt whose missed questionId does NOT exist in content renders without
      throwing (the unknown id is skipped) — passes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>QuizFlow drives select→run→results, converts perDomain to a flat pct map, and records one attempt per finish; HistoryScreen lists attempts (empty-state when none) and opens an attempt's review with missed questions rebuilt from content (non-empty review; unknown ids skipped, no crash).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire routes in App.tsx + update App.test Quiz/History assertions</name>
  <files>src/App.tsx, src/App.test.tsx</files>
  <read_first>
    - src/App.tsx (FULL — route union + screen switch; currently quiz→QuizBrowse, history→Placeholder)
    - src/App.test.tsx (FULL — the Quiz-nav test asserting "Question bank" and History expecting "Coming soon")
    - src/screens/QuizFlow.tsx, src/screens/HistoryScreen.tsx (created Task 2; their visible headings)
    - .planning/phases/04-quiz-engine-modes/04-CONTEXT.md TEST-BREAKAGE TRAP (line 69)
  </read_first>
  <behavior>
    - Quiz nav now renders ModeSelect (heading "Choose a mode"), NOT the QuizBrowse "Question bank".
    - History nav now renders the history view (heading "Quiz history"), NOT the "Coming soon" placeholder.
    - All other App.test cases (initial render, Flashcards nav, theme toggle/persistence, reset) keep
      passing unchanged.
  </behavior>
  <action>
    In src/App.tsx:
    - Replace `import { QuizBrowse } ...` usage: `route === 'quiz'` → `<QuizFlow />`; import QuizFlow
      from './screens/QuizFlow'. (Leave QuizBrowse file in repo, like FlashcardsBrowse — just stop routing to it.)
    - `route === 'history'` → `<HistoryScreen />`; import HistoryScreen from './screens/HistoryScreen'.
      Remove the History Placeholder usage (Placeholder import may stay if unused elsewhere — remove if now unused to keep typecheck clean).

    In src/App.test.tsx (TEST-BREAKAGE TRAP — carve out ONLY these two, keep the rest verbatim):
    - The "switches to Quiz browse catalog" test: rename intent to ModeSelect. Replace the assertions
      `getByText('Question bank')` → assert the ModeSelect heading is present, e.g.
      `expect(screen.getByRole('heading', { name: /Choose a mode/i })).toBeInTheDocument()`, and keep
      `expect(screen.queryByText('Coming soon')).not.toBeInTheDocument()`. Keep the aria-current check.
    - The reset test currently navigates Settings; no change needed there. There is no standalone
      History-nav test, but the History route must no longer show "Coming soon" — add a small test:
      click the History nav tab and assert `getByRole('heading', { name: /Quiz history/i })` is present
      and `queryByText('Coming soon')` is absent.
    Do NOT alter the initial-render, Flashcards-nav, theme, persistence, or reset assertions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'QuizFlow' src/App.tsx` >= 1 and `grep -c 'HistoryScreen' src/App.tsx` >= 1
    - `grep -c "route === 'quiz'" src/App.tsx` === 1 mapping to QuizFlow
    - `grep -c 'Question bank' src/App.test.tsx` === 0 (old Quiz assertion removed)
    - `grep -c 'Coming soon' src/App.test.tsx` >= 1 (still asserted ABSENT for quiz + history)
    - `grep -c 'Choose a mode' src/App.test.tsx` >= 1 and `grep -c 'Quiz history' src/App.test.tsx` >= 1
    - `npm test` FULL suite passes (all prior tests intact, two updated, one added)
    - `npm run typecheck` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>Quiz route → ModeSelect flow, History route → history view; App.test Quiz+History assertions updated, full suite + typecheck + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → History/Results | Rendered attempt data is reload-persistent and tamperable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | HistoryScreen rendering quizHistory | mitigate | Values rendered as text only (no innerHTML); readState default `[]` + schemaVersion guard prevents crash on corrupt history; missed-question rebuild skips unknown/tampered questionIds rather than crashing |
| T-04-06 | Repudiation | attempt id/date generation | accept | Single-user local trainer; id from crypto.randomUUID fallback, date display-only — no audit requirement |
</threat_model>

<verification>
- `npm test` FULL suite passes — QuizResults + History component tests (incl. non-empty rebuilt review + unknown-id skip) + updated App.test + all prior.
- `npm run typecheck` and `npm run build` exit 0 (proves engine-object vs stored-flat perDomain shapes are never crossed).
- `grep -nE 'Math\.random|Date\.now' src/lib/quiz.ts` STILL zero matches (engine purity preserved).
- Manual sanity (optional): finishing a quiz adds a row to History; reopening it shows the missed review; reset-all clears it.
</verification>

<success_criteria>
- After any quiz the user sees score + per-domain breakdown + missed review (QUIZ-06).
- Finished attempts are recorded and viewable in the History view; opening one shows its review with missed questions rebuilt from content (non-empty) (QUIZ-07).
- perDomain shape conversion (engine object → flat pct map) is explicit and typecheck-enforced everywhere it crosses into storage/DomainBars.
- Quiz route runs ModeSelect→Runner→Results; History route replaces the placeholder; no pre-existing tests broken.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-engine-modes/04-03-SUMMARY.md`
</output>
</content>
</invoke>
