---
phase: 04-quiz-engine-modes
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/quiz.ts
  - src/lib/quiz.test.ts
  - src/lib/storage.ts
  - src/lib/storage.test.ts
  - src/lib/useStoredState.ts
autonomous: true
requirements: [QUIZ-01, QUIZ-02, QUIZ-03, QUIZ-04, QUIZ-05, QUIZ-07]
must_haves:
  truths:
    - "scaledScore maps raw correct/total to the 100–1000 band and isPass gates at 720"
    - "gradeAttempt counts unanswered as wrong, computes per-domain pct, and lists missed questions"
    - "selectQuestions deterministically produces the right question set per mode given an injected rng"
    - "The pure timer reports remaining seconds and expiry given an injected now"
    - "A finished attempt round-trips through cae-trainer:v1 and reset clears it"
  artifacts:
    - path: "src/lib/quiz.ts"
      provides: "Pure quiz engine: scaledScore, isPass, gradeAttempt, selectQuestions, remainingSeconds, isExpired"
      min_lines: 80
    - path: "src/lib/quiz.test.ts"
      provides: "Unit tests covering every acceptance hook in 04-CONTEXT <specifics>"
    - path: "src/lib/storage.ts"
      provides: "PersistedState.quizHistory: QuizAttempt[] (default [])"
      contains: "quizHistory"
  key_links:
    - from: "src/lib/useStoredState.ts"
      to: "src/lib/storage.ts"
      via: "patchState({ quizHistory })"
      pattern: "quizHistory"
    - from: "src/lib/quiz.ts"
      to: "purity"
      via: "no Math.random / no Date.now inside the module"
      pattern: "Math\\.random|Date\\.now"
---

<objective>
Build the pure, injected quiz engine (`src/lib/quiz.ts`) and add additive `quizHistory`
persistence to the storage layer. This is the testable core that the UI (Plan 04-02 / 04-03)
drives: scoring, grading, per-mode question selection, and a pure countdown timer — all
deterministic because randomness (`rng`) and the clock (`now`) are injected, never read inside
the module.

Purpose: Mirror the Phase-3 now-injection + additive-persistence pattern (see 03-01-SUMMARY)
so quiz logic is unit-tested independently of React, and history survives refresh.
Output: `src/lib/quiz.ts` + `src/lib/quiz.test.ts`; `PersistedState.quizHistory` and a
`useStoredState` accessor to record attempts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quiz-engine-modes/04-CONTEXT.md
@.planning/phases/03-flashcards-spaced-repetition/03-01-SUMMARY.md

<interfaces>
<!-- Types and selectors the engine builds on. Use directly — no exploration needed. -->

From src/data/domains.ts:
```typescript
export type DomainId = 'd1' | 'd2' | 'd3' | 'd4' | 'd5'
export const DOMAINS: Domain[]   // each { id, code, short, name, weight }
```

From src/data/types.ts:
```typescript
export type Scenario =
  | 'Customer Support Resolution Agent' | 'Code Generation with Claude Code'
  | 'Multi-Agent Research System' | 'Developer Productivity with Claude'
  | 'Claude Code for Continuous Integration' | 'Structured Data Extraction'

export interface Question {
  id: string
  domain: DomainId
  scenario?: Scenario
  stem: string
  options: [string, string, string, string]
  correct: 0 | 1 | 2 | 3
  whyCorrect: string
  whyOthers: string
  source?: 'official-sample' | 'original'
}
```

From src/data/scenarios.ts:
```typescript
export const SCENARIOS = [ /* 6 scenario strings */ ] as const
export type ScenarioName = typeof SCENARIOS[number]
```

From src/data/content.ts:
```typescript
export function getQuestions(opts?: { domain?: DomainId; scenario?: Scenario }): Question[]
```

From src/lib/storage.ts (EXISTING — extend additively, do NOT rewrite):
```typescript
export interface PersistedState {
  schemaVersion: number       // stays 1
  themePref: ThemePref
  srs: Record<string, SrsCard>
}
const DEFAULT_STATE: PersistedState = { schemaVersion: 1, themePref: 'system', srs: {} }
export function readState(): PersistedState      // merges DEFAULT_STATE → new fields always present
export function patchState(patch: Partial<PersistedState>): PersistedState
export function clearAll(): void
```

Data facts (verified): 40 questions; all 6 scenarios populated (min 2 each); every domain d1..d5 has questions.

PURITY RULE (mirrors srs.ts): never call `Math.random()` or `Date.now()` inside src/lib/quiz.ts.
The 03-01 grep-gate trap: basic grep `.` is a wildcard, so `Date.now` also matches `Date(now`.
Name the injected timer parameter `nowMs` (not `now`) and write `new Date(...)` is NOT used here at all,
so the gate `grep -E 'Math\.random|Date\.now' src/lib/quiz.ts` must return zero matches.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure scoring, grading, and timer — quiz.ts (RED→GREEN)</name>
  <files>src/lib/quiz.ts, src/lib/quiz.test.ts</files>
  <read_first>
    - src/lib/srs.ts (now-injection style to mirror exactly)
    - src/lib/srs.test.ts (test layout/style to mirror)
    - src/data/types.ts (Question), src/data/domains.ts (DomainId, DOMAINS)
    - .planning/phases/04-quiz-engine-modes/04-CONTEXT.md <specifics> (acceptance hooks)
  </read_first>
  <behavior>
    scaledScore(correct, total):
      - scaledScore(10,10) === 1000
      - scaledScore(7,10)  === 730  (pass)
      - scaledScore(6,10)  === 640  (fail)
      - scaledScore(0,10)  === 100
      - scaledScore(0,0)   === 100  (guard: total 0 → ratio treated as 0)
    isPass(scaled):
      - isPass(720) === true
      - isPass(719) === false
      - isPass(1000) === true
    gradeAttempt(questions, answers):
      - answers is Record<questionId, number | null> (selectedIndex; null/missing = unanswered = wrong)
      - returns { correct, total, perDomain, missed }
      - unanswered/null counts as wrong (NOT correct, NOT skipped)
      - total === questions.length
      - perDomain[d] = { correct, total, pct } where pct = Math.round(correct/total*100)
      - perDomain only includes domains present in `questions`
      - missed === exactly the Question objects answered wrong or unanswered, in question order
      - Example: 2 questions both d1, answers={q1:correct, q2:null} → correct 1, total 2,
        perDomain.d1 = {correct:1,total:2,pct:50}, missed = [q2]
    remainingSeconds(startedAtMs, durationSec, nowMs):
      - remainingSeconds(s, 600, s+60000) === 540
      - remainingSeconds(s, 600, s) === 600
      - never returns negative: remainingSeconds(s, 600, s+700000) === 0
    isExpired(startedAtMs, durationSec, nowMs):
      - isExpired(s, 600, s+600000) === true
      - isExpired(s, 600, s+599000) === false
  </behavior>
  <action>
    RED first: create src/lib/quiz.test.ts with the behavior cases above (use small inline
    Question fixtures, e.g. `{ id:'q1', domain:'d1', stem:'', options:['','','',''], correct:0, whyCorrect:'', whyOthers:'' }`).
    Run `npm test -- quiz` and confirm it FAILS (functions not yet defined). Commit:
    `test(04-01): add failing tests for quiz scoring/grading/timer`.

    GREEN: create src/lib/quiz.ts (NO imports of Math.random/Date; import only types/DOMAINS):
    - `export function scaledScore(correct: number, total: number): number` →
      `Math.round(100 + (total === 0 ? 0 : correct / total) * 900)`.
    - `export const PASS_MARK = 720` and `export function isPass(scaled: number): boolean` → `scaled >= PASS_MARK`.
    - `export type Answers = Record<string, number | null>`.
    - `export interface DomainScore { correct: number; total: number; pct: number }`.
    - `export interface GradeResult { correct: number; total: number; perDomain: Partial<Record<DomainId, DomainScore>>; missed: Question[] }`.
    - `export function gradeAttempt(questions: Question[], answers: Answers): GradeResult` — iterate
      questions; `const ok = answers[q.id] === q.correct`; accumulate per-domain correct/total; push to
      `missed` when `!ok`; compute pct per domain with `Math.round(correct/total*100)`.
    - `export function remainingSeconds(startedAtMs: number, durationSec: number, nowMs: number): number`
      → `Math.max(0, durationSec - Math.floor((nowMs - startedAtMs) / 1000))`.
    - `export function isExpired(startedAtMs: number, durationSec: number, nowMs: number): boolean`
      → `remainingSeconds(startedAtMs, durationSec, nowMs) <= 0`.
    Run `npm test -- quiz` until green. Commit: `feat(04-01): implement pure quiz scoring/grading/timer`.
  </action>
  <verify>
    <automated>npm test -- quiz</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- quiz` passes with the exact assertion values above (scaledScore(7,10)===730, isPass(720)===true, etc.)
    - `grep -nE 'Math\.random|Date\.now' src/lib/quiz.ts` returns ZERO matches (purity gate)
    - `grep -c 'export function scaledScore' src/lib/quiz.ts` === 1
    - `grep -c 'export function gradeAttempt' src/lib/quiz.ts` === 1
    - `grep -c 'export function remainingSeconds' src/lib/quiz.ts` === 1
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>scaledScore/isPass/gradeAttempt/remainingSeconds/isExpired implemented, all hook tests pass, no clock/random reads in the module.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Deterministic per-mode question selection — selectQuestions (RED→GREEN)</name>
  <files>src/lib/quiz.ts, src/lib/quiz.test.ts</files>
  <read_first>
    - src/data/content.ts (getQuestions), src/data/scenarios.ts (SCENARIOS — the canonical 6)
    - design/screens-quiz.jsx lines 5-27 (reference buildQuestions; NOTE: it hardcoded 4 scenarios and used Math.random — DO NOT copy that; we draw 4 of 6 via injected rng)
    - .planning/phases/04-quiz-engine-modes/04-CONTEXT.md <decisions> mode table + <specifics>
  </read_first>
  <behavior>
    selectQuestions(mode, opts, pool, rng) where rng: () => number (returns [0,1)):
      - 'scenario': draws exactly 4 DISTINCT scenarios from the 6, returns the pool questions for
        those 4 scenarios GROUPED (all questions of scenario A, then all of scenario B, ...).
        Given a fixed rng, the 4 chosen scenarios are deterministic. Result has questions from
        exactly 4 distinct scenario values (assert new Set(result.map(q=>q.scenario)).size === 4).
      - 'domain' with opts={domain:'d2'}: returns only pool questions where domain==='d2', all of them.
      - 'timed': returns exactly 10 questions drawn across domains (shuffle pool with rng, take 10).
        result.length === 10.
      - 'free' with opts={n:7}: returns exactly 7 questions (shuffle pool with rng, take 7). length===7.
      - Determinism: same rng sequence → same result (call with a seeded counter rng in tests).
  </behavior>
  <action>
    RED: extend src/lib/quiz.test.ts. Build the pool from `getQuestions()` (real 40). Use a
    deterministic rng helper in the test, e.g. a linear sequence:
    `const seq = [0.1,0.4,0.7,0.2,0.9,0.5,...]; let i=0; const rng=()=>seq[i++ % seq.length]`.
    Assert the four hooks above. Run `npm test -- quiz` (expect FAIL for selectQuestions). Commit:
    `test(04-01): add failing tests for selectQuestions per mode`.

    GREEN: add to src/lib/quiz.ts:
    - `export type QuizMode = 'scenario' | 'domain' | 'timed' | 'free'`.
    - `export interface SelectOpts { domain?: DomainId; n?: number; scenarios?: Scenario[] }`.
    - A pure `shuffle<T>(arr: T[], rng: () => number): T[]` using Fisher–Yates driven by `rng`
      (returns a new array; never mutates input; never calls Math.random).
    - `export function selectQuestions(mode: QuizMode, opts: SelectOpts, pool: Question[], rng: () => number): Question[]`:
      * 'scenario': `const all = Array.from(new Set(pool.map(q=>q.scenario).filter(Boolean)))` →
        shuffle(all, rng), take first 4 → for each chosen scenario in order, append
        `pool.filter(q => q.scenario === s)` (grouping). Return concatenation.
      * 'domain': `pool.filter(q => q.domain === opts.domain)`.
      * 'timed': `shuffle(pool, rng).slice(0, 10)`.
      * 'free': `shuffle(pool, rng).slice(0, opts.n ?? 10)`.
    Run `npm test -- quiz` until green. Commit: `feat(04-01): implement deterministic selectQuestions`.
  </action>
  <verify>
    <automated>npm test -- quiz</automated>
  </verify>
  <acceptance_criteria>
    - selectQuestions('scenario', {}, pool, rng) → `new Set(result.map(q=>q.scenario)).size === 4`
    - selectQuestions('domain', {domain:'d2'}, pool, rng) → every `q.domain === 'd2'`
    - selectQuestions('timed', {}, pool, rng).length === 10
    - selectQuestions('free', {n:7}, pool, rng).length === 7
    - Two calls with identical rng sequences return identical id arrays (determinism test passes)
    - `grep -nE 'Math\.random|Date\.now' src/lib/quiz.ts` STILL returns ZERO matches
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>selectQuestions covers all 4 modes deterministically with grouped scenario output; purity intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Additive quizHistory persistence (storage + useStoredState)</name>
  <files>src/lib/storage.ts, src/lib/storage.test.ts, src/lib/useStoredState.ts</files>
  <read_first>
    - src/lib/storage.ts (FULL — PersistedState, DEFAULT_STATE, readState merge, patchState, clearAll)
    - src/lib/storage.test.ts (existing test style + the 03-01 trap: existing writeState calls must include all required fields)
    - src/lib/useStoredState.ts (FULL — pattern for srs/rateCard/setSrs/resetAll)
    - .planning/phases/04-quiz-engine-modes/04-CONTEXT.md QuizAttempt shape (lines 60-66)
  </read_first>
  <behavior>
    - readState() on a fresh/empty store returns quizHistory === [] (DEFAULT_STATE merge).
    - addAttempt(attempt) PREPENDS the attempt to quizHistory and persists it.
    - A stored state written then re-read round-trips quizHistory (survives refresh).
    - resetAll()/clearAll() removes the key → next readState().quizHistory === [].
    - schemaVersion stays 1 (additive, backward compatible: old state without quizHistory reads as []).
  </behavior>
  <action>
    In src/lib/storage.ts:
    - Add `export interface QuizAttempt { id: string; date: string; mode: string; modeKey: 'scenario'|'domain'|'timed'|'free'; correct: number; total: number; scaled?: number; pass?: boolean; perDomain: Record<DomainId, number>; missed?: Array<{ questionId: string; selected: number | null }> }`
      (import DomainId from ../data/domains).
    - Add `quizHistory: QuizAttempt[]` to `PersistedState` and `quizHistory: []` to `DEFAULT_STATE`.
      schemaVersion stays 1. readState already spreads DEFAULT_STATE so the default `[]` is always present.
    - 03-01 TRAP: any existing `writeState({...})` / state object literals in storage.test.ts that
      omit the now-required `quizHistory` field will fail typecheck — add `quizHistory: []` to each
      (same fix the 03-01 summary describes for `srs`). Do NOT change their assertions.

    In src/lib/useStoredState.ts:
    - Add `quizHistory` to the in-memory state (init from `readState().quizHistory`).
    - Add `addAttempt(attempt: QuizAttempt): void` → `const next = [attempt, ...readState().quizHistory]; patchState({ quizHistory: next }); setQuizHistoryState(next)`.
    - resetAll(): also reset quizHistory state to `[]`.
    - Export `quizHistory` and `addAttempt` from the hook's return object and its return type.

    In src/lib/storage.test.ts add tests for: fresh read → [], addAttempt prepend + round-trip
    through STORAGE_KEY, clearAll → [].
  </action>
  <verify>
    <automated>npm test -- storage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'quizHistory' src/lib/storage.ts` >= 3 (interface field + default + type usage)
    - `grep -c 'QuizAttempt' src/lib/storage.ts` >= 2
    - `grep -c 'addAttempt' src/lib/useStoredState.ts` >= 2 (definition + export)
    - `grep -c 'SCHEMA_VERSION = 1' src/lib/storage.ts` === 1 (schemaVersion unchanged)
    - New test: fresh readState().quizHistory deep-equals [] — passes
    - New test: addAttempt prepends and round-trips through localStorage — passes
    - `npm test` (full suite) passes; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>quizHistory persists additively, defaults to [], prepends on addAttempt, clears on reset; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | Persisted quizHistory is attacker/tamper-controllable across reloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | readState() quizHistory | mitigate | DEFAULT_STATE spread + schemaVersion check + try/catch (existing storage.ts) yields `[]` for corrupt/absent history rather than crashing; history is render-only data |
| T-04-02 | DoS (quota) | writeState growing quizHistory | accept | Single-user local app; attempt records are small; existing writeState try/catch degrades gracefully on quota |
</threat_model>

<verification>
- `npm test` (full suite) passes — new quiz.test.ts + storage tests + all prior tests.
- `grep -nE 'Math\.random|Date\.now' src/lib/quiz.ts` → zero matches.
- `npm run typecheck` exits 0; `npm run build` exits 0.
</verification>

<success_criteria>
- All 04-CONTEXT <specifics> scoring/selection/timer hooks pass as unit tests.
- quizHistory persists additively (schemaVersion stays 1), prepends on finish, clears on reset.
- Engine is pure/injected — no wall-clock or RNG reads inside src/lib/quiz.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-engine-modes/04-01-SUMMARY.md`
</output>
