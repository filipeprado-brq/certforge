---
phase: 03-flashcards-spaced-repetition
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/screens/FlashcardsStudy.tsx
  - src/screens/FlashcardsStudy.test.tsx
  - src/screens/Dashboard.tsx
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements: [FLASH-01, FLASH-02, FLASH-04, FLASH-05]
must_haves:
  truths:
    - "User can flip a flashcard to reveal the back (concept/answer)"
    - "User can rate recall Again/Good; the rating persists and the card advances out of the queue (and the next time it appears its scheduled state reflects the rating)"
    - "User can start a study session limited to cards due today, optionally filtered to one domain"
    - "User sees a Deck Overview with per-domain learned + due counts and a Start-session CTA respecting the domain filter"
    - "Dashboard shows REAL per-domain flashcard progress derived from SRS state (not the static 0% placeholder)"
  artifacts:
    - path: "src/screens/FlashcardsStudy.tsx"
      provides: "Deck Overview + Study Session (flip, Again/Good rate→persist→advance, progress, done state) gated by view state"
      min_lines: 120
      exports: ["FlashcardsStudy"]
    - path: "src/screens/Dashboard.tsx"
      provides: "Per-domain mastery rows driven by deckStatsByDomain (learned/total %, due count)"
      contains: "deckStatsByDomain"
  key_links:
    - from: "src/screens/FlashcardsStudy.tsx"
      to: "src/lib/srs.ts"
      via: "nextState(prev, rating, Date.now()) on rate"
      pattern: "nextState\\("
    - from: "src/screens/FlashcardsStudy.tsx"
      to: "src/lib/useStoredState (rateCard)"
      via: "persist rated card"
      pattern: "rateCard\\("
    - from: "src/screens/FlashcardsStudy.tsx"
      to: "src/lib/deckStats.ts"
      via: "buildDueQueue / deckStatsByDomain"
      pattern: "buildDueQueue\\(|deckStatsByDomain\\("
    - from: "src/App.tsx"
      to: "src/screens/FlashcardsStudy.tsx"
      via: "flashcards route renders FlashcardsStudy"
      pattern: "FlashcardsStudy"
    - from: "src/screens/Dashboard.tsx"
      to: "src/lib/deckStats.ts"
      via: "deckStatsByDomain(getFlashcards(), srs, Date.now())"
      pattern: "deckStatsByDomain\\("
---

<objective>
Turn the tested SRS core (Plan 01) into the end-to-end study loop: a Deck Overview with per-domain
learned/due counts and a Start-session CTA, a Study Session that builds the due queue, flips cards,
rates Again/Good (persisting via the Plan-01 scheduler) and advances, plus real per-domain progress
on the Dashboard. Wire the Flashcards route to lead with the study loop; keep all 5 routes working.

Purpose: This is the user-facing payoff of Phase 3 — the candidate can actually study with spaced
repetition. It is a vertical slice (Date.now() → scheduler → persistence → UI) built entirely on the
already-tested pure modules, so the UI stays thin and the scheduling logic is never re-implemented here.

Output:
- `src/screens/FlashcardsStudy.tsx` — Deck Overview (default view) + Study Session view, gated by local state.
- `src/screens/Dashboard.tsx` — per-domain mastery driven by real SRS-derived stats.
- `src/App.tsx` — `flashcards` route renders FlashcardsStudy (replaces FlashcardsBrowse as the lead view).
- Component tests: flip reveal + rate-advances-queue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-flashcards-spaced-repetition/03-CONTEXT.md
@.planning/phases/01-app-shell-persistence/01-UI-SPEC.md
@design/screens-flashcards.jsx

<interfaces>
<!-- Plan 01 modules (this plan's dependency) + existing components/CSS. Use directly. -->

From src/lib/srs.ts (Plan 01 — pure, inject Date.now() from the UI):
```typescript
export type Box = 1 | 2 | 3 | 4 | 5
export type Rating = 'again' | 'good'
export interface SrsCard { box: Box; dueAt: number; lastRated: number; rating: Rating }
export function nextState(prev: SrsCard | undefined, rating: Rating, now: number): SrsCard
export function isDue(card: SrsCard | undefined, now: number): boolean
export function intervalDays(box: Box): number
export function startOfDay(now: number): number
```

From src/lib/deckStats.ts (Plan 01):
```typescript
export interface DomainStat { total: number; learned: number; due: number }
export function deckStatsByDomain(cards: Flashcard[], srs: Record<string, SrsCard>, now: number): Record<DomainId, DomainStat>
export function buildDueQueue(cards: Flashcard[], srs: Record<string, SrsCard>, now: number, domain?: DomainId): string[]
export function overallDueCount(cards: Flashcard[], srs: Record<string, SrsCard>, now: number, domain?: DomainId): number
```

From src/lib/useStoredState.ts (Plan 01 — extended):
```typescript
export function useStoredState(): {
  themePref: ThemePref
  setThemePref: (t: ThemePref) => void
  resetAll: () => void
  srs: Record<string, SrsCard>
  setSrs: (next: Record<string, SrsCard>) => void
  rateCard: (id: string, card: SrsCard) => void
}
```

From src/data/content.ts: `getFlashcards(opts?: { domain?: DomainId }): Flashcard[]`
From src/data/domains.ts: `DOMAINS: Domain[]`, `type DomainId`
From src/data/types.ts: `interface Flashcard { id; domain; front; back }`

Existing components (reuse — do NOT restyle):
```typescript
// src/components/Btn.tsx
export const Btn = (props: { variant?: 'filled'|'outline'|'ghost'|'danger'|'danger-filled'; size?: 'sm'|'lg'; arrow?: boolean } & ButtonHTMLAttributes) => ...
// src/components/DomainBadge.tsx
export const DomainBadge = ({ domain }: { domain: Domain }) => ...
export const WeightChip = ({ domain }: { domain: Domain }) => ...
// src/components/ProgressBar.tsx
export const ProgressBar = ({ value, color?, height?, label }: ...) => ...   // role=progressbar
// src/components/EmptyState.tsx
export const EmptyState = ({ title, body, action? }) => ...
// src/components/icons.tsx
export const IconFlip = (p) => ...
```

Existing CSS classes in src/styles/app.css (reuse verbatim — no new CSS): `.flashcard-stage`, `.flashcard`, `.flashcard.flipped` (rotateY + prefers-reduced-motion handled), `.face`, `.face--front`, `.face--back`, `.rating-row` (1fr 1fr grid), `.card`, `.card--flush`, `.row-list`, `.chip-row`, `.filter-chip`, `.page-head`, `.page-kicker`, `.page-title`, `.lead`, `.t-mono`, `.t-mono-sm`, `.t-subtle`, `.t-display`, `.stat-num`, `.spacer`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FlashcardsStudy.tsx — Deck Overview + Study Session study loop</name>
  <files>src/screens/FlashcardsStudy.tsx</files>
  <read_first>
    - design/screens-flashcards.jsx (DeckOverview + StudySession reference layouts — port the interaction and class usage, NOT the window.* CDN globals or domainById helper; use imports + DOMAINS.find instead)
    - .planning/phases/03-flashcards-spaced-repetition/03-CONTEXT.md (the `<decisions>` "Study session" + "Deck Overview" blocks: due queue order, flip→back→rating, session progress + remaining, done state; learned = box>=3; Start CTA respects due + domain)
    - src/screens/FlashcardsBrowse.tsx (existing filter-chip + per-domain summary pattern to mirror)
    - src/lib/srs.ts, src/lib/deckStats.ts, src/lib/useStoredState.ts (Plan 01 contracts — see <interfaces>)
  </read_first>
  <action>
    Create `src/screens/FlashcardsStudy.tsx` exporting `export const FlashcardsStudy = () => { ... }`. It is a single screen with two views gated by local state (`view: 'deck' | 'session'`), NOT a router change.

    State + data:
    - `const { srs, rateCard } = useStoredState()`.
    - `const [filter, setFilter] = useState<'all' | DomainId>('all')` (domain filter, mirrors FlashcardsBrowse chip-row).
    - `const [view, setView] = useState<'deck' | 'session'>('deck')`.
    - `const allCards = getFlashcards()` (full deck; stable reference).
    - Compute `now = Date.now()` at render for stats display (this is the ONLY wall-clock read — pure modules receive it).

    DECK OVERVIEW view (view === 'deck'):
    - page-head: kicker "Flashcards · Spaced repetition", title "Card decks", a `.lead` describing Leitner ("rate Again or Good and cards resurface at the right moment").
    - chip-row: "All" + 5 domain chips (`filter-chip` with ` active` on the selected) — clicking sets `filter`.
    - `const stats = deckStatsByDomain(allCards, srs, now)`. For each domain in DOMAINS (or just the filtered one if filter !== 'all'): a row in a `.card.card--flush > .row-list` with DomainBadge + domain.name + WeightChip + a ProgressBar(value = total ? learned/total*100 : 0, color = `var(--${d.id})`) + text "`{learned} / {total} learned`" and "`{due} due today`" (use stats[d.id]).
    - Start CTA: `const sessionDomain = filter === 'all' ? undefined : filter`; `const dueCount = overallDueCount(allCards, srs, now, sessionDomain)`. A `Btn variant="filled" arrow` labelled `Study {dueCount} due` that sets `view('session')`. If `dueCount === 0`, render an EmptyState ("All caught up" / "No cards are due right now — come back later or pick another domain.") and disable/omit the Start button so a session is never started with an empty queue.

    STUDY SESSION view (view === 'session'):
    - Build the queue ONCE on entering the session so rating a card removes it from THIS session even though its new dueAt is the future: hold `const [queue, setQueue] = useState<string[]>([])` and an `useEffect`/initializer that, when entering session, sets `queue = buildDueQueue(allCards, srs, Date.now(), sessionDomain)`. Capture `const total = queue.length at session start` in state (e.g. `sessionTotal`). Track `const [pos, setPos] = useState(0)` and `const [flipped, setFlipped] = useState(false)`.
    - Current card: `const card = allCards.find(c => c.id === queue[pos])`.
    - Session header: a `← Exit` ghost Btn (`btn btn--ghost btn--sm`) that returns to view 'deck'; a `.spacer`; `t-mono` "`{pos + 1} / {sessionTotal}`"; and remaining-due text "`{sessionTotal - pos} due`". A ProgressBar(value = sessionTotal ? pos/sessionTotal*100 : 0, label "Session progress").
    - Card stage: `<div className="flashcard-stage"><div className={`flashcard${flipped ? ' flipped' : ''}`}>`. Front face (`.face.face--front`) is a button (onClick → setFlipped(true)) showing DomainBadge + card.front + an IconFlip hint "Tap or press space to flip". Back face (`.face.face--back`, aria-hidden={!flipped}) shows DomainBadge + card.front + card.back.
    - Rating row (only when `flipped`): `<div className="rating-row">` with Btn variant="outline" "Again" and Btn variant="filled" "Good". On click of either:
      - `const updated = nextState(srs[card.id], rating, Date.now())` (rating is 'again' | 'good').
      - `rateCard(card.id, updated)` (persists + updates store).
      - `setFlipped(false)`, then advance: `setPos(p => p + 1)`.
    - DONE state: when `pos >= sessionTotal` (queue exhausted), render a centered `.card` "Session complete" with the count rated, an EmptyState-style summary, and two Btns: "Back to decks" (→ view 'deck', reset pos/flipped) and "Study again" (rebuild queue from current srs + Date.now(), reset pos). If the rebuilt queue is empty, show "All caught up" instead.
    - Keyboard (optional but match design): space/Enter toggles flip; when flipped, key "1" rates again, "2" rates good; Escape exits. Use a single useEffect keydown listener.

    Reuse the BRQ flashcard CSS verbatim (.flashcard.flipped handles the rotateY + prefers-reduced-motion). Do NOT add new CSS or a new UI-SPEC. Do NOT import any quiz-engine/timer/scoring/history code (out of scope — Phase 4).

    Covers FLASH-01 (flip reveal), FLASH-02 (Again/Good rating drives scheduling), FLASH-04 (session limited to due cards, optional domain filter), FLASH-05 (per-domain learned + due counts in Deck Overview).
  </action>
  <acceptance_criteria>
    - File exists + exports the screen: `test -f src/screens/FlashcardsStudy.tsx && grep -Eq 'export const FlashcardsStudy' src/screens/FlashcardsStudy.tsx`
    - Uses the Plan-01 scheduler + persistence (no re-implemented scheduling): `grep -Eq 'nextState\(' src/screens/FlashcardsStudy.tsx && grep -Eq 'rateCard\(' src/screens/FlashcardsStudy.tsx`
    - Builds the due queue with optional domain + reads per-domain stats: `grep -Eq 'buildDueQueue\(' src/screens/FlashcardsStudy.tsx && grep -Eq 'deckStatsByDomain\(' src/screens/FlashcardsStudy.tsx`
    - Reuses the BRQ flip/rate CSS verbatim: `grep -Eq 'flashcard-stage' src/screens/FlashcardsStudy.tsx && grep -Eq "flipped" src/screens/FlashcardsStudy.tsx && grep -Eq 'rating-row' src/screens/FlashcardsStudy.tsx && grep -Eq 'face--front' src/screens/FlashcardsStudy.tsx && grep -Eq 'face--back' src/screens/FlashcardsStudy.tsx`
    - Reuses shared components, defines no new CSS: `grep -Eq 'DomainBadge' src/screens/FlashcardsStudy.tsx && grep -Eq 'ProgressBar' src/screens/FlashcardsStudy.tsx`
    - No quiz-engine scope creep: `grep -Eqi 'timer|secondsLeft|scaled|computeResult|attemptHistory|quizMode' src/screens/FlashcardsStudy.tsx` returns NOTHING (exit 1 / empty)
    - Date.now injected (UI is the clock source): `grep -Eq 'Date.now\(\)' src/screens/FlashcardsStudy.tsx`
    - Typecheck clean: `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>FlashcardsStudy renders Deck Overview (per-domain learned/due via deckStatsByDomain, domain filter, Start CTA respecting due+domain) and a Study Session (due queue, flip reveal, Again/Good → nextState→rateCard→advance, session progress + remaining, done state) reusing BRQ flashcard CSS; no quiz scope; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire flashcards route + Dashboard real per-domain SRS progress (with component tests)</name>
  <files>src/App.tsx, src/screens/Dashboard.tsx, src/App.test.tsx, src/screens/FlashcardsStudy.test.tsx</files>
  <read_first>
    - src/App.tsx (the file you are modifying — replace the flashcards-route render; keep all other routes intact)
    - src/screens/Dashboard.tsx (the file you are modifying — replace the static mastery rows + placeholder counts)
    - src/App.test.tsx (existing 7 App tests — the current flashcards test expects "All" chip + "Card deck"; you are changing the lead flashcards view to FlashcardsStudy whose title is "Card decks", so update that one assertion)
    - src/screens/FlashcardsStudy.tsx (Task 1 — the component under test)
    - src/lib/deckStats.ts, src/lib/srs.ts, src/lib/useStoredState.ts (Plan 01 contracts)
    - .planning/phases/03-flashcards-spaced-repetition/03-CONTEXT.md (the "Wiring" + "Dashboard" decisions: Flashcards leads with Deck Overview→Session; Dashboard mastery = real SRS-derived learned/total + due)
  </read_first>
  <behavior>
    Write src/screens/FlashcardsStudy.test.tsx (Testing Library + user-event). Use beforeEach `localStorage.clear()` so each test starts fresh (all cards unseen ⇒ all due).
    - Test "Deck Overview shows per-domain learned and due for a fresh user": render <FlashcardsStudy />, assert the title "Card decks" is present and that "0 / " (learned=0) text and a "due today" label appear (fresh user: learned 0, all due).
    - Test "flip reveals the back": click the Start CTA to enter the session, find the front face, click it (or press space), assert the back-face concept text for the first card becomes visible / `.flashcard` gains the `flipped` class (assert `document.querySelector('.flashcard.flipped')` is non-null after the click).
    - Test "rating Good advances the queue and persists": enter session, flip, click "Good", assert the session counter advances (e.g. "2 /" appears OR the previously-shown front text is gone) AND `JSON.parse(localStorage.getItem('cae-trainer:v1')).srs` has one entry with `rating: 'good'` and `box: 2` (unseen→Good→box2 per Plan 01). This proves rate→persist→advance end to end.

    Update src/App.test.tsx: the existing "switches to Flashcards browse catalog" test asserts the "All" chip / "Card deck" title. Change ONLY that assertion to match the new lead view — assert the Deck Overview title "Card decks" (note the plural) is present after clicking the Flashcards nav tab, and that "Coming soon" is NOT present. Leave the other 6 App tests untouched.
  </behavior>
  <action>
    In src/App.tsx:
    - Replace `import { FlashcardsBrowse } from './screens/FlashcardsBrowse'` usage in the `flashcards` route with `import { FlashcardsStudy } from './screens/FlashcardsStudy'`; render `<FlashcardsStudy />` when `route === 'flashcards'`. (You may keep the FlashcardsBrowse file in the repo unused, or leave its import removed — do NOT delete the file; per 03-CONTEXT the browse view may be retained as "browse all", but the Flashcards route now LEADS with the study loop.) Quiz route (`<QuizBrowse />`) and History route (`<Placeholder title="History" />`) stay exactly as-is.

    In src/screens/Dashboard.tsx (replace the static mastery placeholder with REAL SRS-derived progress — per 03-CONTEXT this is FLASH-05 on the home screen):
    - Add imports: `import { getFlashcards } from '../data/content'`, `import { deckStatsByDomain } from '../lib/deckStats'`, `import { useStoredState } from '../lib/useStoredState'`.
    - Inside the component: `const { srs } = useStoredState()`; `const cards = getFlashcards()`; `const stats = deckStatsByDomain(cards, srs, Date.now())`; `const totalCards = cards.length`.
    - Replace the `totalCards` placeholder line (`DOMAINS.reduce((s) => s + 0, 0)`) so the lead copy shows the real card count.
    - In the Flashcards entry card, replace the `—` stat-num with the count of cards due across all domains (sum of stats[d].due) or totalCards — pick the due count and label it "cards due to study" (fall back to totalCards with "cards ready to learn" only when nothing is due). The Quiz entry card stays `—` (Phase 4 owns quiz stats).
    - In the "Domain mastery" card's row-list: for each domain `d`, compute `const s = stats[d.id]`; render `ProgressBar value={s.total ? (s.learned / s.total) * 100 : 0}` (replace the hardcoded `value={0}`), the right-aligned percent text from the same value (replace the hardcoded `0%`), and add a `t-mono-sm` "`{s.due} due`" indicator. Replace the "No data yet" subtitle with a learned/total summary line.
    - Keep DomainBadge, WeightChip, ProgressBar visuals and the BRQ layout intact — only the numbers change from static to SRS-derived.

    Covers FLASH-04/FLASH-01/FLASH-02 wiring (route leads with study loop) and FLASH-05 (real per-domain progress on Dashboard).
  </action>
  <acceptance_criteria>
    - App route wired to the study loop: `grep -Eq 'FlashcardsStudy' src/App.tsx && grep -Eq "route === 'flashcards'" src/App.tsx`
    - Quiz + History routes unchanged: `grep -Eq 'QuizBrowse' src/App.tsx && grep -Eq 'Placeholder title="History"' src/App.tsx`
    - Dashboard now derives real per-domain progress: `grep -Eq 'deckStatsByDomain\(' src/screens/Dashboard.tsx && grep -Eq 'useStoredState' src/screens/Dashboard.tsx && grep -Eq 'getFlashcards\(' src/screens/Dashboard.tsx`
    - Static placeholders removed from Dashboard: `grep -Eq 'value=\{0\}' src/screens/Dashboard.tsx` returns NOTHING (exit 1); `grep -Eq 's + 0' src/screens/Dashboard.tsx` returns NOTHING (exit 1)
    - Component tests exist + cover flip + rate-persist-advance: `test -f src/screens/FlashcardsStudy.test.tsx && grep -Eqi 'flip' src/screens/FlashcardsStudy.test.tsx && grep -Eqi 'good' src/screens/FlashcardsStudy.test.tsx && grep -Eq "rating" src/screens/FlashcardsStudy.test.tsx`
    - App test updated to the new lead view: `grep -Eq 'Card decks' src/App.test.tsx`
    - Full suite passes: `npm test` exits 0
    - Typecheck + build pass: `npm run typecheck && npm run build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run typecheck && npm run build</automated>
  </verify>
  <done>Flashcards route renders FlashcardsStudy (study loop leads); Quiz/History routes unchanged; Dashboard mastery + card counts derive from deckStatsByDomain over real SRS state (no static 0%/—); FlashcardsStudy.test.tsx proves flip + Good→box2 persist + queue advance; App.test.tsx updated for "Card decks"; npm test/typecheck/build all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (rating clicks) → persisted SRS | Rating buttons write through the typed nextState→rateCard path |
| localStorage → Dashboard/Deck render | SRS map read back to compute progress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | FlashcardsStudy rating path | mitigate | Ratings are typed 'again'\|'good' literals passed to the pure nextState (which clamps box to 1..5); the UI never writes arbitrary box/dueAt values directly, so a malformed rating cannot produce an invalid SrsCard. |
| T-03-04 | Information Disclosure | Dashboard/Deck render of card text | accept | All flashcard text is embedded static content rendered as JSX children (React auto-escapes); no user-supplied content, no dangerouslySetInnerHTML. |
</threat_model>

<verification>
Run after both tasks:
- `npm test` — full suite passes (Plan 01 SRS/stats/storage tests + new FlashcardsStudy component tests + updated App tests).
- `npm run typecheck` — exits 0.
- `npm run build` — exits 0, emits dist/index.html.
- Manual sanity (optional): `npm run dev`, open Flashcards → Deck Overview shows per-domain due counts → Start session → flip a card → rate Good → counter advances; refresh → progress persists; Settings → Reset all → Deck Overview shows all cards due again, 0 learned.
</verification>

<success_criteria>
- User can flip a flashcard to reveal the back (FLASH-01).
- User can rate Again/Good; the rating persists via the Leitner scheduler and the card advances (FLASH-02).
- User can start a study session limited to due cards, optionally filtered to one domain (FLASH-04).
- Deck Overview shows per-domain learned (box>=3) + due counts with a domain-aware Start CTA (FLASH-04, FLASH-05).
- Dashboard shows real per-domain progress derived from SRS state, replacing the static placeholder (FLASH-05).
- All 5 routes remain navigable; Quiz/History unchanged. `npm test`, `npm run typecheck`, `npm run build` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-flashcards-spaced-repetition/03-02-SUMMARY.md`
</output>
