---
phase: 06-question-bank-expansion
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/data/questions.ts
autonomous: true
requirements: [EXP-02, EXP-04, EXP-05, EXP-06]
must_haves:
  truths:
    - "The question bank gains 39 new exam-style questions: 23 D1 (q29-q51) and 16 D2 (q52-q67)"
    - "After this plan d1 questions >= 34 and d2 questions >= 23"
    - "New scenario tags land: Customer Support +2 and Developer Productivity +3 (and any natural Multi-Agent)"
    - "At least 6 of the new questions set hasSnippet: true with a real code/config snippet in the stem"
    - "Every new question has exactly 1 correct + 3 plausible distractors with substantive whyCorrect and whyOthers"
  artifacts:
    - path: "src/data/questions.ts"
      provides: "q29-q67: 23 D1 + 16 D2 original questions"
      contains: "id: 'q67'"
  key_links:
    - from: "new questions"
      to: "getQuestions({domain}) / getQuestions({scenario})"
      via: "domain + scenario tags on each object"
      pattern: "domain: 'd1'"
---

<objective>
Author 39 new exam-style questions in `src/data/questions.ts`: 23 for Domain 1 (ids q29-q51) and 16 for Domain 2 (ids q52-q67). These are the largest two domains. Within them, land the Customer Support (+2) and a first batch of Developer Productivity (+3) scenario tags toward the EXP-04 per-scenario >=8 goal, and at least 6 `hasSnippet: true` questions toward the EXP-05 >=15 goal. After this plan d1 reaches 34 and d2 reaches 23 (both above their test minimums).

Purpose: Deliver the bulk of EXP-02 volume for the two heaviest domains and begin closing EXP-04/EXP-05, mirroring the per-domain authoring shape of Phase 5 (05-02).
Output: q29-q67 appended to QUESTIONS; partial scenario + snippet progress; question total reaches 79.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-question-bank-expansion/06-CONTEXT.md
@.planning/phases/02-exam-content/02-CONTEXT.md
@design/data.jsx

<interfaces>
<!-- Question object shape to append (src/data/types.ts) — hasSnippet added in 06-01 -->
```typescript
interface Question {
  id: string
  domain: DomainId          // 'd1' | 'd2' | 'd3' | 'd4' | 'd5'
  scenario?: Scenario       // one of the 6 SCENARIOS strings, exact match
  stem: string
  options: [string, string, string, string]
  correct: 0 | 1 | 2 | 3
  whyCorrect: string
  whyOthers: string
  source?: Source           // use 'original' for all new questions
  hasSnippet?: boolean      // true when stem embeds a code/config snippet
}
```
Existing pattern (q1) for voice/format: see src/data/questions.ts top of file. Append new objects before the closing `]` of the QUESTIONS array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 23 new Domain 1 questions (q29-q51)</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (read q1-q14 + sq1-sq12 for voice/format; locate the closing `]` to append before it)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (D1 row: +23 q29-q51; EXP-06 quality bar; EXP-05 snippet topics; the correct-answer themes)
    - .planning/phases/02-exam-content/02-CONTEXT.md (official-sample voice + D1 task statements 1.1-1.7)
  </read_first>
  <action>
    Append 23 new `Question` objects with `domain: 'd1'`, contiguous ids `q29` through `q51`, `source: 'original'`, to the QUESTIONS array.

    Ground D1 questions in the agent-vs-workflow / orchestration task statements (1.1-1.7): when to use an agent vs a deterministic workflow; orchestrator-worker vs prompt-chaining vs routing vs parallelization; deterministic enforcement of hard constraints OUTSIDE the model (approval gates in the tool, not the prompt); least-privilege tool access; context isolation across subagents; attention dilution in large contexts; human-in-the-loop placement. Favor tradeoff/judgment framing matching the exam correct-answer themes ("deterministic enforcement > prompt", "right architecture for the dependency structure", "least-privilege").

    Scenario tags (exact-string match to the Scenario type) within these 23:
    - Tag 2 questions `scenario: 'Customer Support Resolution Agent'` (D1 naturally exercises CS: approval gates, escalation, guardrails).
    - Tag 1-2 questions `scenario: 'Multi-Agent Research System'` (orchestrator-worker, subagent context isolation) — natural fit, counts toward keeping that pool strong.

    Snippet questions within these 23: set `hasSnippet: true` on at least 3 questions whose `stem` embeds a readable snippet (use `\n` for line breaks inside the string, keep it short). Cover D1-relevant snippet topics from 06-CONTEXT:
    - an `AgentDefinition` / `allowedTools` config that includes `"Task"` (subagent spawning)
    - an agentic-loop response showing `stop_reason` ("tool_use" vs "end_turn")
    - a `CLAUDE.md` snippet with an `@import` directive (project memory / context discipline)

    Each question: exactly 1 correct + 3 plausible-but-wrong distractors (the kind a half-prepared candidate would pick — not absurd); substantive `whyCorrect` AND `whyOthers` that name why each distractor is wrong, grounded in the guide concept. No duplicate stems; vary the angle across the 23.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q29'" src/data/questions.ts` returns 1 and `grep -c "id: 'q51'" src/data/questions.ts` returns 1 (range present)
    - `grep -oE "id: 'q(2[9]|3[0-9]|4[0-9]|5[01])'" src/data/questions.ts | sort -u | wc -l` returns 23 (q29-q51 all present, unique)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>23 D1 questions q29-q51 appended, typecheck green, scenario + snippet tags applied as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Author 16 new Domain 2 questions (q52-q67)</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (the file being modified — including q29-q51 just added; append before the closing `]`)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (D2 row: +16 q52-q67; EXP-05 snippet topics for D2; Developer Productivity scenario mapping d2/d3)
    - .planning/phases/02-exam-content/02-CONTEXT.md (D2 task statements 2.1-2.5)
  </read_first>
  <action>
    Append 16 new `Question` objects with `domain: 'd2'`, contiguous ids `q52` through `q67`, `source: 'original'`, after q51.

    Ground D2 questions in task statements 2.1-2.5: context engineering and context discipline; provenance / source attribution; tool-result formatting; `tool_choice` modes (`"auto"` / `"any"` / `{type:"tool", name}`); few-shot vs zero-shot; explicit criteria + few-shot > self-reported confidence; MCP server scope (project vs user) and `.mcp.json` configuration; prompt/context structure for reliability. Favor tradeoff framing ("better tool descriptions > more complexity", "explicit criteria + few-shot > confidence self-report", "context discipline/provenance").

    Scenario tags within these 16:
    - Tag 3 questions `scenario: 'Developer Productivity with Claude'` (DevProd maps to d2/d3 — context engineering, prompting workflows, MCP in the dev loop). This is the first half of the DevProd +6 needed; the rest land in 06-03.

    Snippet questions within these 16: set `hasSnippet: true` on at least 3 whose `stem` embeds a readable snippet. Cover D2 snippet topics from 06-CONTEXT:
    - a `.mcp.json` config with `${ENV}` variable expansion (and contrast project vs user scope)
    - a `tool_choice` example showing `"auto"` vs `"any"` vs `{type:"tool", name:"..."}`
    - a `CLAUDE.md` snippet (project memory / instructions structure)

    Same quality bar: 1 correct + 3 plausible distractors, substantive whyCorrect + whyOthers, no duplicates, varied angles.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q52'" src/data/questions.ts` returns 1 and `grep -c "id: 'q67'" src/data/questions.ts` returns 1
    - `grep -oE "id: 'q(5[2-9]|6[0-7])'" src/data/questions.ts | sort -u | wc -l` returns 16 (q52-q67 all present, unique)
    - `grep -c "domain: 'd1'" src/data/questions.ts` returns >= 34 (11 original + 23 new)
    - `grep -c "domain: 'd2'" src/data/questions.ts` returns >= 23 (7 original + 16 new)
    - `grep -c "scenario: 'Developer Productivity with Claude'" src/data/questions.ts` returns >= 5 (2 original + >=3 new)
    - `grep -c "hasSnippet: true" src/data/questions.ts` returns >= 6 (>=3 from D1 task + >=3 here)
    - `npm run typecheck` exits 0
    - `npm test` shows d1 (>=32) and d2 (>=22) per-domain question gates now GREEN
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test 2>&1 | grep -E "(d1 >= 32|d2 >= 22|PASS|FAIL)" | head</automated>
  </verify>
  <done>16 D2 questions q52-q67 appended; d1>=34, d2>=23; DevProd +3 + 3 D2 snippets applied; typecheck green; d1/d2 count gates green.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0.
- `grep -c "domain: 'd1'"` >= 34; `grep -c "domain: 'd2'"` >= 23.
- `grep -c "hasSnippet: true"` >= 6.
- d1 (>=32) and d2 (>=22) per-domain test gates GREEN; total still < 120 (d3/d4/d5 pending).
</verification>

<success_criteria>
- 39 new questions q29-q67 (23 D1 + 16 D2), all source 'original', exam-style.
- Customer Support +2, Multi-Agent +1-2, Developer Productivity +3 scenario tags applied.
- >= 6 hasSnippet questions covering AgentDefinition/Task, stop_reason, CLAUDE.md/@import, .mcp.json/${ENV}, tool_choice.
- d1 and d2 per-domain gates green; question total 79.
</success_criteria>

<output>
After completion, create `.planning/phases/06-question-bank-expansion/06-02-SUMMARY.md`
</output>
