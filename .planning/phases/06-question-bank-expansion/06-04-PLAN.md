---
phase: 06-question-bank-expansion
plan: "04"
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/data/questions.ts
autonomous: true
requirements: [EXP-02, EXP-04, EXP-05, EXP-06, EXP-07]
must_haves:
  truths:
    - "The question bank gains 13 new exam-style D5 questions (q102-q114), reaching d5 >= 19 and total >= 126"
    - "Structured Data Extraction scenario reaches >= 9 tagged questions (+5 new here) — buffer target; test gate stays >=8"
    - "Every one of the 6 official scenarios has >= 9 scenario-tagged questions (buffer target; content.test.ts >=8 satisfied with margin)"
    - "At least 15 questions across the bank have hasSnippet: true"
    - "The full content.test.ts suite is GREEN: >=120 total, per-domain mins, each scenario >=8, hasSnippet >=15, official-sample === 12, shape/id/no-fetch invariants, and all Phase-5 flashcard gates"
    - "typecheck + build + npm test all pass"
  artifacts:
    - path: "src/data/questions.ts"
      provides: "q102-q114: 13 D5 questions; final bank >= 126 questions"
      contains: "id: 'q114'"
  key_links:
    - from: "full question bank"
      to: "content.test.ts gates (count/scenario/snippet/invariants)"
      via: "getQuestions selectors"
      pattern: "Structured Data Extraction"
---

<objective>
Author the final 13 new exam-style questions in `src/data/questions.ts`: 13 for Domain 5 (ids q102-q114), reaching d5 >= 19 and the bank >= 126 total. Land the last scenario batch — Structured Data Extraction (+5) — so every one of the 6 scenarios lands at >= 9 tagged questions (a +1 buffer past the content.test.ts >=8 gate). Top off `hasSnippet: true` questions so the bank has >= 15. Then close out EXP-07: the FULL content.test.ts suite GREEN (all question count/coverage/snippet gates + preserved invariants + Phase-5 flashcard gates), and typecheck + build + npm test all pass.

Note on scenario buffer: each scenario carries a +1 buffer (target >=9) so a single dropped or misspelled scenario tag still leaves the pool at >=8 and the EXP-04 gate green. The content.test.ts assertion remains >=8 — do NOT change it. The buffer tags go on questions already being authored (or on existing domain-only new questions during top-off), never increasing per-domain counts.

Purpose: Complete EXP-02/04/05/06 and drive the EXP-07 RED gate (set in 06-01) fully GREEN — the final v1.1 deliverable.
Output: q102-q114 appended; all 6 scenarios >=9 (gate >=8); >=15 snippets; entire suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-question-bank-expansion/06-CONTEXT.md
@.planning/phases/02-exam-content/02-CONTEXT.md
@design/data.jsx

<interfaces>
<!-- Append D5 questions before the closing `]` of QUESTIONS. -->
```typescript
interface Question {
  id: string
  domain: DomainId          // 'd5'
  scenario?: Scenario       // 'Structured Data Extraction' (exact) for the +5
  stem: string
  options: [string, string, string, string]
  correct: 0 | 1 | 2 | 3
  whyCorrect: string
  whyOthers: string
  source?: Source           // 'original'
  hasSnippet?: boolean
}
```
content.test.ts gates to satisfy (set RED in 06-01): total >=120; per-domain {d1:32,d2:22,d3:24,d4:24,d5:18}; each of 6 SCENARIOS >=8 (we target >=9 via buffer); hasSnippet >=15; official-sample === 12; 4-option/correct/whyCorrect/whyOthers shape; unique ids; no fetch; plus Phase-5 flashcard gates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 13 new Domain 5 questions (q102-q114) incl. Structured Data +5</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (the file being modified — q29-q101 present; append before the closing `]`)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (D5 row: +13 q102-q114; Structured Data maps to d4/d5; D5 snippet topics; EXP-06 quality bar)
    - .planning/phases/02-exam-content/02-CONTEXT.md (D5 task statements 5.1-5.6)
  </read_first>
  <action>
    Append 13 new `Question` objects with `domain: 'd5'`, contiguous ids `q102` through `q114`, `source: 'original'`.

    Ground D5 questions in task statements 5.1-5.6: evaluation and grading of agent/LLM systems; observability and tracing; cost/latency/quality tradeoffs and monitoring; safety, guardrails, and red-teaming; iterating on prompts/agents with metrics; production reliability and rollout. Favor tradeoff framing ("explicit grading criteria + few-shot > self-reported confidence", "measure before optimizing", "structured eval > vibes").

    Scenario tags within these 13 (buffer-aware; tags go on D5 questions you are already authoring — D5 count unchanged):
    - Tag 5 questions `scenario: 'Structured Data Extraction'` (was +4; +1 buffer; Structured Data maps to d4/d5; extraction quality, schema validation, eval of extracted fields fit D5 evaluation). This completes Structured Data buffered: 4 original + 5 new = 9.

    Snippet questions within these 13: set `hasSnippet: true` on at least 3 whose `stem` embeds a readable snippet. Cover remaining D5/D4-adjacent snippet topics not yet used (check what 06-02/06-03 already used — keep distinct):
    - a JSON Schema with `enum` + `nullable` (extraction schema for an eval)
    - a structured eval/error record (`errorCategory`, `isRetryable`) or a grading rubric config
    - a Message Batches request with `custom_id` (batch eval throughput) if not already used in D4

    NO-FETCH SNIPPET CAVEAT (CONT-06 gate): no embedded snippet may contain the literal substrings `fetch(`, `import(`, `axios`, or `XHR` anywhere in src/data/*. Avoid JS `fetch(...)`, dynamic-`import(...)`, `axios`, or `XHR` samples; for any import example use `@import ./file.md` WITHOUT a paren. Otherwise the no-fetch gate false-positive-fails on questions.ts.

    Quality bar: 1 correct + 3 plausible distractors, substantive whyCorrect + whyOthers, no duplicates.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q102'" src/data/questions.ts` returns 1 and `grep -c "id: 'q114'" src/data/questions.ts` returns 1
    - `grep -oE "id: 'q(10[2-9]|11[0-4])'" src/data/questions.ts | sort -u | wc -l` returns 13
    - `grep -c "domain: 'd5'" src/data/questions.ts` returns >= 19 (6 original + 13 new)
    - `grep -c "scenario: 'Structured Data Extraction'" src/data/questions.ts` returns >= 9 (4 original + 5 new) — buffer target
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>13 D5 questions q102-q114 appended; d5>=19; Structured Data pool reaches 9 (buffer); >=3 D5 snippets containing no fetch(/import(/axios/XHR; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Close out EXP-07 — verify all gates GREEN, top off snippets/scenarios if short</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (the full file — to count/adjust scenario tags and hasSnippet flags)
    - src/data/content.test.ts (the gates that must pass — do NOT edit this file in this task; it is authoritative)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (the &lt;specifics&gt; acceptance hooks — final checklist)
  </read_first>
  <action>
    This is the green-closeout task. Run the full suite and confirm every Phase 6 gate passes. If any gate is short, FIX BY EDITING questions.ts ONLY (do not weaken content.test.ts):

    1. Run `npm test` and inspect failures.
    2. Buffer check: each of the 6 scenario pools should land at >= 9 (the +1 buffer past the >=8 test gate). If any pool is < 9 (and especially if any is < 8): add the missing scenario tag to existing untagged new questions whose topic naturally fits that scenario (do NOT invent new ids — re-tag domain-only practice questions among q29-q114 that fit). Verify the mapping is natural (CS->d1/d2/d5; Multi-Agent->d1/d2/d5; Code Gen & CI->d3/d4; Dev Productivity->d2/d3; Structured Data->d4/d5). The content.test.ts assertion stays >=8; the >=9 buffer is the planning target, not a test change.
    3. If hasSnippet count < 15: set `hasSnippet: true` on additional existing new questions that already embed (or, by adding a short snippet to the stem, can embed) a readable code/config snippet, drawing from the 06-CONTEXT topic list. Reach >= 15. Any snippet you add or touch MUST NOT contain the literal substrings `fetch(`, `import(`, `axios`, or `XHR` (no-fetch gate) — use `@import ./file.md` WITHOUT a paren for import examples.
    4. Confirm no duplicate ids, every new question has exactly 4 options + correct 0..3 + non-empty whyCorrect/whyOthers, and `source: 'original'` on all q29-q114.
    5. Confirm the 12 official samples (sq1-sq12, `source: 'official-sample'`) and q1-q28 are byte-unchanged (no edits to those objects).
    6. Confirm flashcards.ts was NOT touched in this phase.

    Then run typecheck + build + full test and confirm all green.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q" src/data/questions.ts` returns >= 114 originals total -> total bank `grep -cE "^\s*id: '" src/data/questions.ts` returns >= 126
    - For EACH scenario the buffer target is >= 9 (test gate >= 8): `grep -c "scenario: 'Customer Support Resolution Agent'"` >= 9, `'Code Generation with Claude Code'` >= 9, `'Multi-Agent Research System'` >= 9, `'Developer Productivity with Claude'` >= 9, `'Claude Code for Continuous Integration'` >= 9, `'Structured Data Extraction'` >= 9 (all must be >= 8 minimum for the suite to pass)
    - `grep -c "hasSnippet: true" src/data/questions.ts` returns >= 15
    - `grep -c "source: 'official-sample'" src/data/questions.ts` returns 12 (official samples preserved)
    - `git status --porcelain src/data/flashcards.ts` shows no change (flashcards untouched) — or confirm flashcards.ts is absent from this phase's diff
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `npm test` exits 0 with ALL tests passing (per-domain mins d1>=32/d2>=22/d3>=24/d4>=24/d5>=18, total >=120, each scenario >=8, hasSnippet >=15, official-sample===12, shape/id/no-fetch invariants, AND all Phase-5 flashcard gates green)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build && npm test</automated>
  </verify>
  <done>All 6 scenarios >=9 (gate >=8); hasSnippet >=15; total >=126; official-sample===12; no snippet contains fetch(/import(/axios/XHR; q1-q28 + sq1-sq12 unchanged; flashcards.ts untouched; typecheck + build + full npm test all GREEN.</done>
</task>

</tasks>

<verification>
- `npm run typecheck && npm run build && npm test` all exit 0.
- `grep -cE "^\s*id: '" src/data/questions.ts` >= 126.
- Each of the 6 scenario `grep -c` counts >= 9 (buffer target; content.test.ts gate stays >=8).
- `grep -c "hasSnippet: true"` >= 15.
- `grep -c "source: 'official-sample'"` === 12.
- Phase-5 flashcard gates still green (no flashcards.ts edits).
</verification>

<success_criteria>
- Question bank >= 126 (>=120 required), per-domain d1>=34/d2>=23/d3>=25/d4>=25/d5>=19 (all above test mins).
- All 6 official scenarios have >= 9 scenario-tagged questions (EXP-04 buffer; gate stays >=8, satisfied with margin).
- >= 15 questions with hasSnippet covering the 06-CONTEXT topic list, none containing fetch(/import(/axios/XHR (EXP-05).
- Every new question exam-style, 1 correct + 3 plausible distractors, grounded explanations (EXP-06).
- content.test.ts fully GREEN with invariants preserved + flashcard gates green; typecheck + build pass (EXP-07).
</success_criteria>

<output>
After completion, create `.planning/phases/06-question-bank-expansion/06-04-SUMMARY.md`
</output>
</output>
