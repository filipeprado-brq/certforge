---
phase: 06-question-bank-expansion
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/data/questions.ts
autonomous: true
requirements: [EXP-02, EXP-04, EXP-05, EXP-06]
must_haves:
  truths:
    - "The question bank gains 34 new exam-style questions: 17 D3 (q68-q84) and 17 D4 (q85-q101)"
    - "After this plan d3 questions >= 25 and d4 questions >= 25"
    - "New scenario tags land: Code Generation +4, Continuous Integration +5, Developer Productivity +4 (buffer-aware: Code Gen -> 9, CI -> 9, DevProd -> 9)"
    - "At least 6 more questions set hasSnippet: true (running total >= 12)"
    - "Every new question has exactly 1 correct + 3 plausible distractors with substantive whyCorrect and whyOthers"
  artifacts:
    - path: "src/data/questions.ts"
      provides: "q68-q101: 17 D3 + 17 D4 original questions"
      contains: "id: 'q101'"
  key_links:
    - from: "new questions"
      to: "getQuestions({scenario}) for Code Gen / CI / Dev Productivity"
      via: "scenario tags on each object"
      pattern: "scenario: 'Claude Code for Continuous Integration'"
---

<objective>
Author 34 new exam-style questions in `src/data/questions.ts`: 17 for Domain 3 (ids q68-q84) and 17 for Domain 4 (ids q85-q101). Within them, land the Code Generation (+4), Continuous Integration (+5), and the second batch of Developer Productivity (+4) scenario tags toward the EXP-04 per-scenario goal, and at least 6 more `hasSnippet: true` questions toward the EXP-05 >=15 goal (running total >=12). After this plan d3 reaches 25 and d4 reaches 25.

Note on scenario buffer: every scenario carries a +1 buffer so each lands at >=9 (one extra past the content.test.ts >=8 gate), protecting against a single dropped or misspelled scenario tag. These extra tags go on questions you are ALREADY authoring for D3/D4 (tag a domain-only question with a scenario instead of leaving it domain-only) — the buffer does NOT increase the per-domain NEW question totals. Per-domain id ranges/counts are unchanged.

Purpose: Deliver EXP-02 volume for D3/D4 and close most of the EXP-04 scenario tagging (only Structured Data remains for 06-04).
Output: q68-q101 appended; scenario + snippet progress; question total reaches 113.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-question-bank-expansion/06-CONTEXT.md
@.planning/phases/02-exam-content/02-CONTEXT.md
@design/data.jsx

<interfaces>
<!-- Question object shape (src/data/types.ts). Append before closing `]` of QUESTIONS. -->
```typescript
interface Question {
  id: string
  domain: DomainId          // use 'd3' then 'd4'
  scenario?: Scenario       // exact-string match to a Scenario value
  stem: string
  options: [string, string, string, string]
  correct: 0 | 1 | 2 | 3
  whyCorrect: string
  whyOthers: string
  source?: Source           // 'original'
  hasSnippet?: boolean
}
```
Scenario strings (exact): 'Code Generation with Claude Code', 'Claude Code for Continuous Integration', 'Developer Productivity with Claude'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 17 new Domain 3 questions (q68-q84)</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (the file being modified — append before the closing `]`; q29-q67 already present)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (D3 row: +17 q68-q84; D3/D4 map to Code Gen & CI; D2/D3 map to Dev Productivity; EXP-05 snippet topics; EXP-06 quality bar)
    - .planning/phases/02-exam-content/02-CONTEXT.md (D3 task statements 3.1-3.6)
  </read_first>
  <action>
    Append 17 new `Question` objects with `domain: 'd3'`, contiguous ids `q68` through `q84`, `source: 'original'`.

    Ground D3 questions in task statements 3.1-3.6: Claude Code workflows; hooks (`PreToolUse`/`PostToolUse`) for deterministic enforcement; `.claude/rules/` path-scoped instructions; SKILL.md skills; CLI usage (`claude -p`, `--output-format json`); subagents/Task tool; permissions and least-privilege in the dev loop; CI integration patterns. Favor tradeoff framing ("deterministic hook enforcement > asking the model nicely", "least-privilege tool grants", "scoped rules > one giant CLAUDE.md").

    Scenario tags within these 17 (buffer-aware; tags go on D3 questions you are already authoring — D3 count unchanged):
    - Tag 4 questions `scenario: 'Code Generation with Claude Code'` (was +3; +1 buffer so Code Gen pool -> 9).
    - Tag 3 questions `scenario: 'Claude Code for Continuous Integration'` (was +2; +1 buffer; CI gets 3 here + 2 in D4 = +5 total -> 9).
    - Tag 4 questions `scenario: 'Developer Productivity with Claude'` (was +3; +1 buffer; completes DevProd buffered +7: 3 from 06-02 + 4 here -> 9).

    Snippet questions within these 17: set `hasSnippet: true` on at least 3 whose `stem` embeds a readable snippet. Cover D3 snippet topics:
    - a `PreToolUse` or `PostToolUse` hook config (deterministic enforcement)
    - a `.claude/rules/` YAML frontmatter with a `paths:` glob
    - a `SKILL.md` frontmatter (`context: fork`, `allowed-tools`, `argument-hint`)

    NO-FETCH SNIPPET CAVEAT (CONT-06 gate): no embedded snippet may contain the literal substrings `fetch(`, `import(`, `axios`, or `XHR` anywhere in src/data/*. For any CLAUDE.md/skill import example use `@import ./file.md` WITHOUT a paren; do not include JS `fetch(...)`, dynamic-`import(...)`, `axios`, or `XHR` samples. Otherwise the no-fetch gate false-positive-fails on questions.ts.

    Quality bar: 1 correct + 3 plausible distractors, substantive whyCorrect + whyOthers, no duplicates.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q68'" src/data/questions.ts` returns 1 and `grep -c "id: 'q84'" src/data/questions.ts` returns 1
    - `grep -oE "id: 'q(6[89]|7[0-9]|8[0-4])'" src/data/questions.ts | sort -u | wc -l` returns 17
    - `grep -c "scenario: 'Code Generation with Claude Code'" src/data/questions.ts` returns >= 9 (5 original + >=4 new) — buffer target
    - `grep -c "scenario: 'Developer Productivity with Claude'" src/data/questions.ts` returns >= 9 (2 original + 3 from 06-02 + >=4 new) — buffer target
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>17 D3 questions q68-q84 appended; Code Gen +4, CI +3, DevProd +4 tagged (buffer-aware); >=3 D3 snippets containing no fetch(/import(/axios/XHR; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Author 17 new Domain 4 questions (q85-q101)</name>
  <files>src/data/questions.ts</files>
  <read_first>
    - src/data/questions.ts (the file being modified — q29-q84 present; append before the closing `]`)
    - .planning/phases/06-question-bank-expansion/06-CONTEXT.md (D4 row: +17 q85-q101; CI maps to d3/d4; Structured Data maps to d4/d5; EXP-05 snippet topics for D4)
    - .planning/phases/02-exam-content/02-CONTEXT.md (D4 task statements 4.1-4.6)
  </read_first>
  <action>
    Append 17 new `Question` objects with `domain: 'd4'`, contiguous ids `q85` through `q101`, `source: 'original'`.

    Ground D4 questions in task statements 4.1-4.6: structured output and JSON Schema (nullable, enum, "other"+detail pattern); the right API for the latency need; Message Batches (`custom_id`) for throughput vs sync API for latency; structured error handling (`isError`, `errorCategory`, `isRetryable`); CLI JSON output (`--output-format json`, `--json-schema`); CI gating and machine-readable output; evaluation/grading. Favor tradeoff framing ("structured errors > generic strings", "right API for the latency/throughput need", "schema-constrained output > free-text parsing").

    Scenario tags within these 17 (buffer-aware; tags go on D4 questions you are already authoring — D4 count unchanged):
    - Tag 2 questions `scenario: 'Claude Code for Continuous Integration'` (completes CI buffered +5: 3 from D3 task + 2 here). After this, CI pool = 4 original + 5 new = 9.

    Snippet questions within these 17: set `hasSnippet: true` on at least 3 whose `stem` embeds a readable snippet. Cover D4 snippet topics:
    - a CLI invocation `claude -p --output-format json --json-schema ...`
    - a JSON Schema with `nullable`/`enum`/an `"other"`+detail field
    - a structured error object (`isError` / `errorCategory` / `isRetryable`) OR a Message Batches request showing `custom_id`

    NO-FETCH SNIPPET CAVEAT (CONT-06 gate): no embedded snippet may contain the literal substrings `fetch(`, `import(`, `axios`, or `XHR` anywhere in src/data/*. Avoid JS `fetch(...)`, dynamic-`import(...)`, `axios`, or `XHR` samples; for any import example use `@import ./file.md` WITHOUT a paren. Otherwise the no-fetch gate false-positive-fails on questions.ts.

    Quality bar: 1 correct + 3 plausible distractors, substantive whyCorrect + whyOthers, no duplicates.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'q85'" src/data/questions.ts` returns 1 and `grep -c "id: 'q101'" src/data/questions.ts` returns 1
    - `grep -oE "id: 'q(8[5-9]|9[0-9]|10[01])'" src/data/questions.ts | sort -u | wc -l` returns 17
    - `grep -c "domain: 'd3'" src/data/questions.ts` returns >= 25 (8 original + 17 new)
    - `grep -c "domain: 'd4'" src/data/questions.ts` returns >= 25 (8 original + 17 new)
    - `grep -c "scenario: 'Claude Code for Continuous Integration'" src/data/questions.ts` returns >= 9 (4 original + 5 new) — buffer target
    - `grep -c "hasSnippet: true" src/data/questions.ts` returns >= 12
    - `npm run typecheck` exits 0
    - `npm test` shows d3 (>=24) and d4 (>=24) per-domain question gates now GREEN
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test 2>&1 | grep -E "(d3 >= 24|d4 >= 24|Continuous Integration|Code Generation)" | head</automated>
  </verify>
  <done>17 D4 questions q85-q101 appended; d3>=25, d4>=25; CI pool reaches 9 (buffer); >=12 total snippets containing no fetch(/import(/axios/XHR; typecheck green; d3/d4 gates green.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` exits 0.
- `grep -c "domain: 'd3'"` >= 25; `grep -c "domain: 'd4'"` >= 25.
- Code Gen scenario >= 9; CI scenario >= 9; Dev Productivity >= 9 (buffer-aware targets; test gate stays >=8).
- `grep -c "hasSnippet: true"` >= 12.
- d3/d4 per-domain gates GREEN; total still < 120 (d5 pending).
</verification>

<success_criteria>
- 34 new questions q68-q101 (17 D3 + 17 D4), exam-style, source 'original'.
- Code Gen +4, CI +5, Dev Productivity +4 scenario tags applied (Code Gen, CI, DevProd pools each reach >=9 buffer target; test gate stays >=8).
- >= 6 more hasSnippet questions (running total >=12) covering hooks, .claude/rules paths, SKILL.md frontmatter, CLI -p/--output-format/--json-schema, JSON Schema nullable/enum, structured error, custom_id — none containing fetch(/import(/axios/XHR.
- d3 and d4 per-domain gates green; question total 113.
</success_criteria>

<output>
After completion, create `.planning/phases/06-question-bank-expansion/06-03-SUMMARY.md`
</output>
</output>
