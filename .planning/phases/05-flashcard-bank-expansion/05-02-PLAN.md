---
phase: 05-flashcard-bank-expansion
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/data/flashcards.ts
autonomous: true
requirements: [EXP-01, EXP-03]
must_haves:
  truths:
    - "Domain 1 has >=43 flashcards (current 13 + 30 NEW), each NEW card domain-tagged d1 with a taskRef in 1.1-1.7"
    - "Domain 2 has >=29 flashcards (current 9 + 20 NEW), each NEW card domain-tagged d2 with a taskRef in 2.1-2.5"
    - "All NEW ids are unique f51+ and continue the existing array; f1-f50 are preserved unchanged"
    - "All seven D1 statements (1.1-1.7) and all five D2 statements (2.1-2.5) appear as a taskRef on >=1 NEW card"
    - "No fetch/import()/axios introduced; cards match the existing f1-f50 voice"
  artifacts:
    - path: "src/data/flashcards.ts"
      provides: "30 new D1 cards + 20 new D2 cards with taskRef"
      contains: "taskRef:"
  key_links:
    - from: "src/data/flashcards.ts FLASHCARDS"
      to: "content.test.ts flashcard d1/d2 minimums + taskRef coverage"
      via: "exported array consumed by getFlashcards"
      pattern: "taskRef:"
---

<objective>
Author the Domain 1 (Agentic Architecture & Orchestration) and Domain 2 (Tool Design & MCP
Integration) flashcard expansion: +30 NEW D1 cards (13 -> >=43) and +20 NEW D2 cards (9 -> >=29),
each NEW card tagged with the `taskRef` of the exam task statement it covers, grounded in the
guide concepts in 05-CONTEXT.md.

These authoring counts intentionally exceed the content.test.ts per-domain MINIMUMS (d1>=40, d2>=27)
by a small buffer so a single dropped or malformed card during a large authoring run does not fail
a `>=` gate. The test minimums in 05-01 stay unchanged; this plan simply authors with margin.

Purpose: Deliver the two largest-weight domains' share of the >=150 deck (EXP-01) and cover all
D1 (1.1-1.7) and D2 (2.1-2.5) task statements (EXP-03).
Output: 50 new entries appended to the FLASHCARDS array in src/data/flashcards.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md
@src/data/flashcards.ts
@design/data.jsx

<interfaces>
Flashcard shape (after 05-01): `{ id: string; domain: DomainId; front: string; back: string; taskRef?: string }`.
Existing D1 cards: f1-f4, f16-f24 (13 total). Existing D2 cards: f5-f7, f25-f30 (9 total).
Highest existing id is f50, so NEW ids start at f51 and increment without gaps or reuse.
Voice reference: terse term-or-question front; 1-3 sentence correct back; see f1-f50 above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 30 NEW Domain 1 flashcards (f51-f80)</name>
  <read_first>
    - src/data/flashcards.ts (D1 block f1-f4, f16-f24 for voice + to avoid duplicate concepts)
    - .planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md (lines 44-51: the seven D1 statements 1.1-1.7 and their sub-concepts)
  </read_first>
  <files>src/data/flashcards.ts</files>
  <action>
    Append 30 NEW d1 flashcards with ids f51 through f80 to the FLASHCARDS array, placed in/after the
    D1 section (keep the array a single valid TS literal). Each card: domain 'd1', a `taskRef` from
    1.1-1.7, a terse front (term or sharp question), and a correct 1-3 sentence back.

    This is +30 (13 -> 43), three cards above the d1>=40 test minimum on purpose — the buffer absorbs
    a dropped/malformed card without failing the gate.

    Distribute across all seven D1 statements so EVERY one of 1.1-1.7 has >=1 NEW card (suggested
    spread totaling 30, vary the angle so no two cards repeat a concept already in f1-f4/f16-f24):
    - 1.1 Agentic loop lifecycle (>=5): stop_reason "tool_use" vs "end_turn"; append tool_result then loop; loop-until-end_turn; anti-pattern: parsing NL for termination; anti-pattern: arbitrary iteration caps.
    - 1.2 Coordinator-subagent orchestration (>=5): hub-and-spoke; isolated subagent context; dynamic subagent selection; partition scope to avoid duplicate work; route all comms through coordinator; iterative refinement.
    - 1.3 Subagent invocation / context passing (>=5): Task tool requires allowedTools include "Task"; explicit context in the prompt (no auto-inheritance); AgentDefinition; fork_session; parallel via multiple Task calls in one turn; goal-not-procedure prompts.
    - 1.4 Multi-step workflow enforcement / handoff (>=4): programmatic prerequisite gates vs prompt guidance; deterministic compliance for financial ops; structured escalation handoff summary (customer id, root cause, recommended action).
    - 1.5 Agent SDK hooks (>=4): PostToolUse for data normalization (Unix/ISO/numeric -> uniform); tool-call interception to block a policy violation (e.g. refund > $500) and redirect; hooks give deterministic guarantees vs prompt's probabilistic compliance.
    - 1.6 Task decomposition strategies (>=4): fixed prompt chaining vs dynamic adaptive decomposition; per-file local pass + cross-file integration pass; adaptive investigation plans.
    - 1.7 Session state / resumption / forking (>=3): `--resume <name>`; fork_session for divergent branches; inform agent of file changes on resume; new-session-with-summary vs resume-with-stale-results.

    Do NOT modify or renumber f1-f50. Do NOT introduce fetch/import()/axios. No duplicate ids.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f5[1-9]'\|id: 'f6[0-9]'\|id: 'f7[0-9]'\|id: 'f80'" src/data/flashcards.ts` returns 30 (ids f51-f80 present)
    - Every NEW card in this range has `domain: 'd1'` and a `taskRef:` line
    - All of 1.1,1.2,1.3,1.4,1.5,1.6,1.7 appear: `for t in 1.1 1.2 1.3 1.4 1.5 1.6 1.7; do grep -q "taskRef: '$t'" src/data/flashcards.ts; done` (each found)
    - `grep -c "fetch(\|import(\|axios" src/data/flashcards.ts` returns 0
    - `npm run typecheck` exits 0
    - `npx vitest run src/data/content.test.ts -t "flashcard count d1"` passes (d1>=40, authored with +3 buffer)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Documents/exame-ant && grep -c "id: 'f5[1-9]'\|id: 'f6[0-9]'\|id: 'f7[0-9]'\|id: 'f80'" src/data/flashcards.ts && for t in 1.1 1.2 1.3 1.4 1.5 1.6 1.7; do grep -q "taskRef: '$t'" src/data/flashcards.ts && echo "ok $t" || echo "MISSING $t"; done && grep -c "fetch(\|import(\|axios" src/data/flashcards.ts && npm run typecheck && npx vitest run src/data/content.test.ts -t "flashcard count d1"</automated>
  </verify>
  <done>30 new d1 cards (f51-f80) appended with taskRefs covering all of 1.1-1.7; d1>=40 with buffer; typecheck green; f1-f50 unchanged; no network primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Author 20 NEW Domain 2 flashcards (f81-f100)</name>
  <read_first>
    - src/data/flashcards.ts (D2 block f5-f7, f25-f30 for voice + to avoid duplicate concepts; confirm highest id is now f80)
    - .planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md (lines 53-58: the five D2 statements 2.1-2.5 and their sub-concepts)
  </read_first>
  <files>src/data/flashcards.ts</files>
  <action>
    Append 20 NEW d2 flashcards with ids f81 through f100 to the FLASHCARDS array, in/after the D2
    section. Each: domain 'd2', a `taskRef` from 2.1-2.5, terse front, 1-3 sentence correct back.

    This is +20 (9 -> 29), two cards above the d2>=27 test minimum on purpose — the buffer absorbs a
    dropped/malformed card without failing the gate.

    Distribute so EVERY one of 2.1-2.5 has >=1 NEW card (suggested spread totaling 20, vary angle vs
    f5-f7/f25-f30):
    - 2.1 Tool interface design (>=4): descriptions are the primary selection mechanism; include input formats/examples/edges/boundaries; rename/split to remove overlap (analyze_content -> extract_web_results); system-prompt keyword sensitivity.
    - 2.2 Structured error responses (>=4): MCP isError; transient vs validation vs business vs permission categories; errorCategory + isRetryable + human-readable; access-failure vs valid-empty-result; attempt local recovery before propagating.
    - 2.3 Tool distribution + tool_choice (>=4): too many tools (18 vs 4-5) degrades selection; scoped per-role tools; scoped cross-role tool (verify_fact); tool_choice "auto"/"any"/forced {type:"tool",name}.
    - 2.4 MCP server integration (>=4): project .mcp.json vs user ~/.claude.json; ${ENV} expansion; tools discovered at connect time; MCP resources as content catalogs; enhance descriptions so MCP tools win over built-in Grep; prefer community servers (e.g. Jira) for standard integrations.
    - 2.5 Built-in tools (>=4): Grep (content search) vs Glob (path patterns); Read/Write (full) vs Edit (unique-match) with Read+Write fallback; incremental codebase understanding (Grep entry points -> Read imports).

    Do NOT modify f1-f80 entries. No fetch/import()/axios. No duplicate ids.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f8[1-9]'\|id: 'f9[0-9]'\|id: 'f100'" src/data/flashcards.ts` returns 20 (ids f81-f100 present)
    - Every NEW card in this range has `domain: 'd2'` and a `taskRef:` line
    - All of 2.1,2.2,2.3,2.4,2.5 appear as taskRefs: `for t in 2.1 2.2 2.3 2.4 2.5; do grep -q "taskRef: '$t'" src/data/flashcards.ts; done`
    - `grep -c "fetch(\|import(\|axios" src/data/flashcards.ts` returns 0
    - `npm run typecheck` exits 0
    - `npx vitest run src/data/content.test.ts -t "flashcard count d2"` passes (d2>=27, authored with +2 buffer)
    - `npx vitest run src/data/content.test.ts -t "all flashcard ids are unique"` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Documents/exame-ant && grep -c "id: 'f8[1-9]'\|id: 'f9[0-9]'\|id: 'f100'" src/data/flashcards.ts && for t in 2.1 2.2 2.3 2.4 2.5; do grep -q "taskRef: '$t'" src/data/flashcards.ts && echo "ok $t" || echo "MISSING $t"; done && grep -c "fetch(\|import(\|axios" src/data/flashcards.ts && npm run typecheck && npx vitest run src/data/content.test.ts -t "flashcard count d2" && npx vitest run src/data/content.test.ts -t "all flashcard ids are unique"</automated>
  </verify>
  <done>20 new d2 cards (f81-f100) appended with taskRefs covering all of 2.1-2.5; d2>=27 with buffer; ids unique; typecheck green; no network primitives.</done>
</task>

</tasks>

<verification>
- d1 count >=40 (authored 43) and d2 count >=27 (authored 29) via the per-domain count tests.
- All twelve D1+D2 statements (1.1-1.7, 2.1-2.5) present as taskRefs.
- f1-f50 preserved; ids unique; no fetch/import()/axios; typecheck green.
</verification>

<success_criteria>
- +30 d1 (f51-f80) and +20 d2 (f81-f100) NEW cards authored with correct domains and taskRefs.
- All D1 (1.1-1.7) and D2 (2.1-2.5) task statements covered.
- Per-domain d1/d2 minimum tests pass with margin; id-uniqueness preserved; typecheck green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-flashcard-bank-expansion/05-02-SUMMARY.md`
</output>
