---
phase: 05-flashcard-bank-expansion
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/data/flashcards.ts
autonomous: true
requirements: [EXP-01, EXP-03]
must_haves:
  truths:
    - "Domain 3 has >=30 flashcards (current 10 + 20 NEW), each NEW card domain-tagged d3 with a taskRef in 3.1-3.6"
    - "Domain 4 has >=30 flashcards (current 10 + 20 NEW), each NEW card domain-tagged d4 with a taskRef in 4.1-4.6"
    - "All NEW ids are unique and continue f101+; f1-f100 are preserved unchanged"
    - "All six D3 statements (3.1-3.6) and all six D4 statements (4.1-4.6) appear as a taskRef on >=1 NEW card"
    - "No fetch/import()/axios introduced; cards match the existing voice"
  artifacts:
    - path: "src/data/flashcards.ts"
      provides: "20 new D3 cards + 20 new D4 cards with taskRef"
      contains: "taskRef:"
  key_links:
    - from: "src/data/flashcards.ts FLASHCARDS"
      to: "content.test.ts flashcard d3/d4 minimums + taskRef coverage"
      via: "exported array consumed by getFlashcards"
      pattern: "taskRef:"
---

<objective>
Author the Domain 3 (Claude Code Configuration & Workflows) and Domain 4 (Prompt Engineering &
Structured Output) flashcard expansion: +20 NEW D3 cards (10 -> >=30) and +20 NEW D4 cards
(10 -> >=30), each NEW card tagged with its `taskRef`, grounded in the 05-CONTEXT guide concepts.

Purpose: Deliver D3 and D4's share of the >=150 deck (EXP-01) and cover all D3 (3.1-3.6) and D4
(4.1-4.6) task statements (EXP-03).
Output: ~40 new entries appended to the FLASHCARDS array in src/data/flashcards.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md
@src/data/flashcards.ts
@design/data.jsx

<interfaces>
Flashcard shape: `{ id: string; domain: DomainId; front: string; back: string; taskRef?: string }`.
Existing D3 cards: f8-f10, f31-f37 (10 total). Existing D4 cards: f11-f13, f38-f44 (10 total).
After 05-02 the highest id is f100, so NEW ids start at f101. Match the f1-f50 voice.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 20 NEW Domain 3 flashcards (f101-f120)</name>
  <read_first>
    - src/data/flashcards.ts (D3 block f8-f10, f31-f37 for voice + to avoid duplicate concepts; confirm highest id is f100)
    - .planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md (lines 60-66: the six D3 statements 3.1-3.6 and their sub-concepts)
  </read_first>
  <files>src/data/flashcards.ts</files>
  <action>
    Append 20 NEW d3 flashcards with ids f101 through f120 to the FLASHCARDS array, in/after the D3
    section. Each: domain 'd3', a `taskRef` from 3.1-3.6, terse front, 1-3 sentence correct back.

    Distribute so EVERY one of 3.1-3.6 has >=1 NEW card (suggested spread totaling 20, vary angle vs
    f8-f10/f31-f37):
    - 3.1 CLAUDE.md hierarchy (>=4): user ~/.claude/CLAUDE.md vs project .claude/CLAUDE.md or root vs directory-level; @import modular includes; .claude/rules/ topic files; /memory to verify loaded files.
    - 3.2 Custom slash commands & skills (>=4): .claude/commands/ (project) vs ~/.claude/commands/ (personal); SKILL.md frontmatter context:fork, allowed-tools, argument-hint; skills (on-demand) vs CLAUDE.md (always-loaded).
    - 3.3 Path-specific rules (>=3): .claude/rules/ YAML frontmatter `paths:`/glob activation; glob rules beat directory CLAUDE.md for files spread across dirs (**/*.test.tsx).
    - 3.4 Plan mode vs direct execution (>=3): plan mode for large/architectural/multi-file; direct for well-scoped single-file; Explore subagent for verbose discovery; combine plan-then-execute.
    - 3.5 Iterative refinement (>=3): concrete I/O examples beat prose; test-driven iteration; interview pattern; single-message (interacting) vs sequential (independent) fixes.
    - 3.6 CI/CD integration (>=3): `-p`/`--print` non-interactive; `--output-format json` + `--json-schema`; CLAUDE.md for CI project context; independent review instance vs same-session self-review.

    Do NOT modify f1-f100. No fetch/import()/axios. No duplicate ids.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f10[1-9]'\|id: 'f11[0-9]'\|id: 'f120'" src/data/flashcards.ts` returns 20 (ids f101-f120 present)
    - Every NEW card in this range has `domain: 'd3'` and a `taskRef:` line
    - All of 3.1..3.6 appear: `for t in 3.1 3.2 3.3 3.4 3.5 3.6; do grep -q "taskRef: '$t'" src/data/flashcards.ts; done`
    - `grep -c "fetch(\|import(\|axios" src/data/flashcards.ts` returns 0
    - `npm run typecheck` exits 0
    - `npx vitest run src/data/content.test.ts -t "flashcard count d3"` passes (d3>=30)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Documents/exame-ant && grep -c "id: 'f10[1-9]'\|id: 'f11[0-9]'\|id: 'f120'" src/data/flashcards.ts && for t in 3.1 3.2 3.3 3.4 3.5 3.6; do grep -q "taskRef: '$t'" src/data/flashcards.ts && echo "ok $t" || echo "MISSING $t"; done && grep -c "fetch(\|import(\|axios" src/data/flashcards.ts && npm run typecheck && npx vitest run src/data/content.test.ts -t "flashcard count d3"</automated>
  </verify>
  <done>20 new d3 cards (f101-f120) appended with taskRefs covering all of 3.1-3.6; d3>=30; typecheck green; f1-f100 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Author 20 NEW Domain 4 flashcards (f121-f140)</name>
  <read_first>
    - src/data/flashcards.ts (D4 block f11-f13, f38-f44 for voice + to avoid duplicate concepts; confirm highest id is f120)
    - .planning/phases/05-flashcard-bank-expansion/05-CONTEXT.md (lines 68-74: the six D4 statements 4.1-4.6 and their sub-concepts)
  </read_first>
  <files>src/data/flashcards.ts</files>
  <action>
    Append 20 NEW d4 flashcards with ids f121 through f140 to the FLASHCARDS array, in/after the D4
    section. Each: domain 'd4', a `taskRef` from 4.1-4.6, terse front, 1-3 sentence correct back.

    Distribute so EVERY one of 4.1-4.6 has >=1 NEW card (suggested spread totaling 20, vary angle vs
    f11-f13/f38-f44):
    - 4.1 Explicit criteria / precision / false positives (>=4): specific categorical criteria beat "be conservative"; false-positive categories erode trust; severity criteria with code examples.
    - 4.2 Few-shot prompting (>=4): most effective for consistent format; demonstrate ambiguous-case handling; generalize to novel patterns; reduce extraction hallucination; 2-4 targeted examples.
    - 4.3 Structured output via tool_use (>=4): tool_use + JSON schema = guaranteed schema-valid (no syntax errors, but semantic errors remain); tool_choice auto/any/forced; optional/nullable fields prevent fabrication; enum + "other"+detail.
    - 4.4 Validation / retry / feedback loops (>=3): retry-with-error-feedback; retries useless when info is absent vs format/structural errors; detected_pattern field; calculated_total vs stated_total / conflict_detected.
    - 4.5 Batch processing (>=3): Message Batches API ~50% cost / up to 24h / no latency SLA; non-blocking workloads only; no multi-turn tool calling in batch; custom_id correlation; submission cadence math.
    - 4.6 Multi-instance / multi-pass review (>=2): self-review limitation (retains its own reasoning); independent instance catches more; per-file + cross-file passes; confidence self-report for routing.

    Do NOT modify f1-f120. No fetch/import()/axios. No duplicate ids.
  </action>
  <acceptance_criteria>
    - `grep -c "id: 'f12[1-9]'\|id: 'f13[0-9]'\|id: 'f140'" src/data/flashcards.ts` returns 20 (ids f121-f140 present)
    - Every NEW card in this range has `domain: 'd4'` and a `taskRef:` line
    - All of 4.1..4.6 appear: `for t in 4.1 4.2 4.3 4.4 4.5 4.6; do grep -q "taskRef: '$t'" src/data/flashcards.ts; done`
    - `grep -c "fetch(\|import(\|axios" src/data/flashcards.ts` returns 0
    - `npm run typecheck` exits 0
    - `npx vitest run src/data/content.test.ts -t "flashcard count d4"` passes (d4>=30)
    - `npx vitest run src/data/content.test.ts -t "all flashcard ids are unique"` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Documents/exame-ant && grep -c "id: 'f12[1-9]'\|id: 'f13[0-9]'\|id: 'f140'" src/data/flashcards.ts && for t in 4.1 4.2 4.3 4.4 4.5 4.6; do grep -q "taskRef: '$t'" src/data/flashcards.ts && echo "ok $t" || echo "MISSING $t"; done && grep -c "fetch(\|import(\|axios" src/data/flashcards.ts && npm run typecheck && npx vitest run src/data/content.test.ts -t "flashcard count d4" && npx vitest run src/data/content.test.ts -t "all flashcard ids are unique"</automated>
  </verify>
  <done>20 new d4 cards (f121-f140) appended with taskRefs covering all of 4.1-4.6; d4>=30; ids unique; typecheck green.</done>
</task>

</tasks>

<verification>
- d3 count >=30 and d4 count >=30 via the per-domain count tests.
- All twelve D3+D4 statements (3.1-3.6, 4.1-4.6) present as taskRefs.
- f1-f100 preserved; ids unique; no fetch/import()/axios; typecheck green.
</verification>

<success_criteria>
- +20 d3 (f101-f120) and +20 d4 (f121-f140) NEW cards authored with correct domains and taskRefs.
- All D3 (3.1-3.6) and D4 (4.1-4.6) task statements covered.
- Per-domain d3/d4 minimum tests pass; id-uniqueness preserved; typecheck green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-flashcard-bank-expansion/05-03-SUMMARY.md`
</output>
